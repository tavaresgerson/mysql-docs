## 12.2 Referência de Função Carregável

A tabela a seguir lista cada função que pode ser carregada em tempo de execução e fornece uma breve descrição de cada uma. Para uma tabela que lista funções e operadores internos, consulte a Seção 12.1, “Referência de Função e Operador Interno”.

Para informações gerais sobre funções carregáveis, consulte a Seção 5.6, “Funções carregáveis do MySQL Server”.

**Tabela 12.2 Funções carregáveis**

<table frame="box" rules="all" summary="A reference that lists all loadable functions."><col style="width: 28%"/><col style="width: 71%"/><thead><tr><th>Name</th> <th>Descrição</th> </tr></thead><tbody><tr><td><code>asymmetric_decrypt()</code></td> <td>Descifre o texto cifrado usando uma chave privada ou pública</td> </tr><tr><td><code>asymmetric_derive()</code></td> <td>Derivar chave simétrica a partir de chaves assimétricas</td> </tr><tr><td><code>asymmetric_encrypt()</code></td> <td>Criptografar texto em claro usando chave privada ou pública</td> </tr><tr><td><code>asymmetric_sign()</code></td> <td>Gerar assinatura a partir do digest</td> </tr><tr><td><code>asymmetric_verify()</code></td> <td>Verifique se a assinatura corresponde ao digest</td> </tr><tr><td><code>audit_log_encryption_password_get()</code></td> <td>Obter senha de criptografia do registro de auditoria</td> </tr><tr><td><code>audit_log_encryption_password_set()</code></td> <td>Definir senha de criptografia do log de auditoria</td> </tr><tr><td><code>audit_log_filter_flush()</code></td> <td>Filtre as tabelas do registro de auditoria</td> </tr><tr><td><code>audit_log_filter_remove_filter()</code></td> <td>Remova o filtro do registro de auditoria</td> </tr><tr><td><code>audit_log_filter_remove_user()</code></td> <td>Desvincular o filtro do registro de auditoria do usuário</td> </tr><tr><td><code>audit_log_filter_set_filter()</code></td> <td>Definir filtro de registro de auditoria</td> </tr><tr><td><code>audit_log_filter_set_user()</code></td> <td>Atribuir filtro de registro de auditoria ao usuário</td> </tr><tr><td><code>audit_log_read()</code></td> <td>Registros do log de auditoria de retorno</td> </tr><tr><td><code>audit_log_read_bookmark()</code></td> <td>Marcar como favorito o evento do registro de auditoria mais recente</td> </tr><tr><td><code>create_asymmetric_priv_key()</code></td> <td>Crie a chave privada</td> </tr><tr><td><code>create_asymmetric_pub_key()</code></td> <td>Crie a chave pública</td> </tr><tr><td><code>create_dh_parameters()</code></td> <td>Gerar segredo de DH compartilhado</td> </tr><tr><td><code>create_digest()</code></td> <td>Gerar digestão a partir de uma string</td> </tr><tr><td><code>flush_rewrite_rules()</code></td> <td>Carregue a tabela rewrite_rules no cache do Rewriter</td> </tr><tr><td><code>gen_blacklist()</code></td> <td>Realizar a substituição de termos do dicionário</td> </tr><tr><td><code>gen_dictionary_drop()</code></td> <td>Remova o dicionário do registro</td> </tr><tr><td><code>gen_dictionary_load()</code></td> <td>Carregar o dicionário no registro</td> </tr><tr><td><code>gen_dictionary()</code></td> <td>Retorne um termo aleatório do dicionário</td> </tr><tr><td><code>gen_range()</code></td> <td>Gerar número aleatório dentro do intervalo</td> </tr><tr><td><code>gen_rnd_email()</code></td> <td>Gerar endereço de e-mail aleatório</td> </tr><tr><td><code>gen_rnd_pan()</code></td> <td>Gerar número de conta principal de cartão de pagamento aleatório</td> </tr><tr><td><code>gen_rnd_ssn()</code></td> <td>Gerar um número de segurança social aleatório dos EUA</td> </tr><tr><td><code>gen_rnd_us_phone()</code></td> <td>Gerar número de telefone aleatório dos EUA</td> </tr><tr><td><code>keyring_aws_rotate_cmk()</code></td> <td>Rotacionar a chave mestre do cliente da AWS</td> </tr><tr><td><code>keyring_aws_rotate_keys()</code></td> <td>Rotacionar as chaves no arquivo de armazenamento keyring_aws</td> </tr><tr><td><code>keyring_key_fetch()</code></td> <td>Pegar o valor da chave do chaveiro</td> </tr><tr><td><code>keyring_key_generate()</code></td> <td>Gerar chave aleatória para chaveiro</td> </tr><tr><td><code>keyring_key_length_fetch()</code></td> <td>Comprimento da chave do chaveiro de retorno</td> </tr><tr><td><code>keyring_key_remove()</code></td> <td>Remova a chave do chaveiro</td> </tr><tr><td><code>keyring_key_store()</code></td> <td>Armazene a chave no chaveiro</td> </tr><tr><td><code>keyring_key_type_fetch()</code></td> <td>Tipo de chave do chaveiro de retorno</td> </tr><tr><td><code>load_rewrite_rules()</code></td> <td>Auxiliar de rotina do plugin de reescritor</td> </tr><tr><td><code>mask_inner()</code></td> <td>Parte interna da máscara de cordas</td> </tr><tr><td><code>mask_outer()</code></td> <td>Máscara esquerda e direita das partes da corda</td> </tr><tr><td><code>mask_pan()</code></td> <td>Número do cartão de pagamento da máscara, parte da cadeia de caracteres</td> </tr><tr><td><code>mask_pan_relaxed()</code></td> <td>Número do cartão de pagamento da máscara, parte da cadeia de caracteres</td> </tr><tr><td><code>mask_ssn()</code></td> <td>Máscara o Número de Segurança Social dos EUA</td> </tr><tr><td><code>mysql_firewall_flush_status()</code></td> <td>Redefinir as variáveis do status do firewall</td> </tr><tr><td><code>normalize_statement()</code></td> <td>Normalize a declaração SQL para digerir o formulário</td> </tr><tr><td><code>read_firewall_users()</code></td> <td>Atualizar o cache do perfil da conta do firewall</td> </tr><tr><td><code>read_firewall_whitelist()</code></td> <td>Atualize o perfil de conta do firewall cache de declaração registrada</td> </tr><tr><td><code>service_get_read_locks()</code></td> <td>Adquira um serviço de bloqueio de fechaduras compartilhadas</td> </tr><tr><td><code>service_get_write_locks()</code></td> <td>Adquira fechaduras de serviço de bloqueio exclusivas</td> </tr><tr><td><code>service_release_locks()</code></td> <td>Liberação de fechaduras de serviço de bloqueio</td> </tr><tr><td><code>set_firewall_mode()</code></td> <td>Estabelecer o modo operacional do perfil da conta do firewall</td> </tr><tr><td><code>version_tokens_delete()</code></td> <td>Exclua tokens da lista de tokens de versão</td> </tr><tr><td><code>version_tokens_edit()</code></td> <td>Lista de tokens de versão modificada</td> </tr><tr><td><code>version_tokens_lock_exclusive()</code></td> <td>Adquira trancas exclusivas nos tokens de versão</td> </tr><tr><td><code>version_tokens_lock_shared()</code></td> <td>Adquira bloqueios compartilhados em tokens de versão</td> </tr><tr><td><code>version_tokens_set()</code></td> <td>Lista de tokens de versão definida</td> </tr><tr><td><code>version_tokens_show()</code></td> <td>Lista de tokens de versão de retorno</td> </tr><tr><td><code>version_tokens_unlock()</code></td> <td>Tokens de versão de liberação de bloqueio</td> </tr></tbody></table>