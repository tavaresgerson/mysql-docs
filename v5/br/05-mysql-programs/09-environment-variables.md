## 4.9 Variáveis de ambiente

Esta seção lista as variáveis de ambiente que são usadas diretamente ou indiretamente pelo MySQL. A maioria dessas variáveis também pode ser encontrada em outros lugares deste manual.

As opções na linha de comando têm precedência sobre os valores especificados em arquivos de opção e variáveis de ambiente, e os valores em arquivos de opção têm precedência sobre os valores em variáveis de ambiente. Em muitos casos, é preferível usar um arquivo de opção em vez de variáveis de ambiente para modificar o comportamento do MySQL. Veja a Seção 4.2.2.2, “Usando Arquivos de Opção”.

<table summary="Variáveis de ambiente que são usadas diretamente ou indiretamente pelo MySQL."><col style="width: 40%"/><col style="width: 60%"/><thead><tr> <th>Variável</th> <th>Descrição</th> </tr></thead><tbody><tr> <td>PH_HTML_CODE_<code>libmysqlclient.so</code>]</td> <td>Nível de registro de autenticação LDAP no lado do cliente.</td> </tr><tr> <td>PH_HTML_CODE_<code>libmysqlclient.so</code>]</td> <td>PAMconfigurações de registro de depuração do plugin de autenticação.</td> </tr><tr> <td>PH_HTML_CODE_<code>mysql_clear_password</code>]</td> <td>O nome do seu compilador C (para execução<span><strong>CMake</strong></span>).</td> </tr><tr> <td>PH_HTML_CODE_<code>LIBMYSQL_PLUGIN_DIR</code>]</td> <td>O nome do seu compilador C++ (para execução<span><strong>CMake</strong></span>).</td> </tr><tr> <td>PH_HTML_CODE_<code>LIBMYSQL_PLUGINS</code>]</td> <td>O nome do seu compilador C (para execução<span><strong>CMake</strong></span>).</td> </tr><tr> <td>PH_HTML_CODE_<code>MYSQL_DEBUG</code>]</td> <td>O nome de usuário padrão para Perl DBI.</td> </tr><tr> <td>PH_HTML_CODE_<code>MYSQL_GROUP_SUFFIX</code>]</td> <td>Opções de rastreamento para Perl DBI.</td> </tr><tr> <td>PH_HTML_CODE_<code>--defaults-group-suffix</code>]</td> <td>O caminho padrão para o<span><strong>mysql</strong></span>o arquivo de histórico é PH_HTML_CODE_<code>MYSQL_HISTFILE</code>].</td> </tr><tr> <td>PH_HTML_CODE_<code>$HOME/.mysql_history</code>]</td> <td>Usado para especificar a localização de <code>libmysqlclient.so</code>.</td> </tr><tr> <td><code>AUTHENTICATION_PAM_LOG</code><code>libmysqlclient.so</code>]</td> <td>Ative o plugin de autenticação <code>mysql_clear_password</code>; vejaSeção 6.4.1.6, “Autenticação Plugável de Texto Aberto no Lado do Cliente”.</td> </tr><tr> <td><code>LIBMYSQL_PLUGIN_DIR</code></td> <td>Diretório onde procurar plugins do cliente.</td> </tr><tr> <td><code>LIBMYSQL_PLUGINS</code></td> <td>Plugins do cliente para pré-carregar.</td> </tr><tr> <td><code>MYSQL_DEBUG</code></td> <td>Opções de rastreamento de depuração durante a depuração.</td> </tr><tr> <td><code>MYSQL_GROUP_SUFFIX</code></td> <td>Valor do sufixo do grupo de opções (como especificar<code>--defaults-group-suffix</code>).</td> </tr><tr> <td><code>MYSQL_HISTFILE</code></td> <td>O caminho para o<span><strong>mysql</strong></span>arquivo de histórico. Se essa variável estiver definida, seu valor substitui o padrão para <code>$HOME/.mysql_history</code>.</td> </tr><tr> <td><code>CC</code><code>libmysqlclient.so</code>]</td> <td>Padrões que especificam declarações que<span><strong>mysql</strong></span>não deve fazer login no <code>CC</code><code>libmysqlclient.so</code>] ou <code>CC</code><code>mysql_clear_password</code>] se<code>CC</code><code>LIBMYSQL_PLUGIN_DIR</code>]é fornecida.</td> </tr><tr> <td><code>CC</code><code>LIBMYSQL_PLUGINS</code>]</td> <td>O caminho para o diretório onde o arquivo específico do servidor <code>CC</code><code>MYSQL_DEBUG</code>] reside.</td> </tr><tr> <td><code>CC</code><code>MYSQL_GROUP_SUFFIX</code>]</td> <td>O nome de host padrão usado pelo<span><strong>mysql</strong></span>cliente de linha de comando.</td> </tr><tr> <td><code>CC</code><code>--defaults-group-suffix</code>]</td> <td>Comprimento máximo da chave para<code>CC</code><code>MYSQL_HISTFILE</code>]VejaSeção 6.6.2, “Uso e exemplos de criptografia da MySQL Enterprise”.</td> </tr><tr> <td><code>CC</code><code>$HOME/.mysql_history</code>]</td> <td>Comprimento máximo da chave DSA para<code>CXX</code><code>libmysqlclient.so</code>]VejaSeção 6.6.2, “Uso e exemplos de criptografia da MySQL Enterprise”.</td> </tr><tr> <td><code>CXX</code><code>libmysqlclient.so</code>]</td> <td>Comprimento máximo da chave RSA para<code>CXX</code><code>mysql_clear_password</code>]VejaSeção 6.6.2, “Uso e exemplos de criptografia da MySQL Enterprise”.</td> </tr><tr> <td><code>CXX</code><code>LIBMYSQL_PLUGIN_DIR</code>]</td> <td>O prompt de comando a ser usado no<span><strong>mysql</strong></span>cliente de linha de comando.</td> </tr><tr> <td><code>CXX</code><code>LIBMYSQL_PLUGINS</code>]</td> <td>A senha padrão ao se conectar a<span><strong>mysqld</strong></span>Usar isso é inseguro. VejaSeção 6.1.2.1, “Diretrizes para o Usuário Final sobre Segurança de Senhas”.</td> </tr><tr> <td><code>CXX</code><code>MYSQL_DEBUG</code>]</td> <td>O número de porta TCP/IP padrão.</td> </tr><tr> <td><code>CXX</code><code>MYSQL_GROUP_SUFFIX</code>]</td> <td>O nome do arquivo de caminho de login <code>CXX</code><code>--defaults-group-suffix</code>].</td> </tr><tr> <td><code>CXX</code><code>MYSQL_HISTFILE</code>]</td> <td>Se o plugin de registro do protocolo de teste faz com que os clientes parem de funcionar. Veja a nota após a tabela.</td> </tr><tr> <td><code>CXX</code><code>$HOME/.mysql_history</code>]</td> <td>Se o plugin de registro do protocolo de teste produz saída. Veja a nota após a tabela.</td> </tr><tr> <td><code>CC</code><code>libmysqlclient.so</code>]</td> <td>O nome padrão do arquivo de soquete Unix; usado para conexões ao <code>CC</code><code>libmysqlclient.so</code>].</td> </tr><tr> <td><code>CC</code><code>mysql_clear_password</code>]</td> <td>O número de porta TCP/IP padrão do X Plugin.</td> </tr><tr> <td><code>CC</code><code>LIBMYSQL_PLUGIN_DIR</code>]</td> <td>O nome padrão do arquivo de soquete Unix do X Plugin; usado para conexões ao <code>CC</code><code>LIBMYSQL_PLUGINS</code>].</td> </tr><tr> <td><code>CC</code><code>MYSQL_DEBUG</code>]</td> <td>Usado pelo shell para encontrar programas MySQL.</td> </tr><tr> <td><code>CC</code><code>MYSQL_GROUP_SUFFIX</code>]</td> <td>Localização de <code>CC</code><code>--defaults-group-suffix</code>]<span><strong>pkg-config</strong></span>arquivo. Veja a nota após a tabela.</td> </tr><tr> <td><code>CC</code><code>MYSQL_HISTFILE</code>]</td> <td>O diretório onde os arquivos temporários são criados.</td> </tr><tr> <td><code>CC</code><code>$HOME/.mysql_history</code>]</td> <td>Isso deve ser ajustado para o seu fuso horário local. VejaSeção B.3.3.7, “Problemas com Fuso Horário”.</td> </tr><tr> <td><code>DBI_USER</code><code>libmysqlclient.so</code>]</td> <td>O modo de criação de arquivos de usuário ao criar arquivos. Veja a nota a seguir na tabela.</td> </tr><tr> <td><code>DBI_USER</code><code>libmysqlclient.so</code>]</td> <td>O modo de criação do diretório do usuário ao criar diretórios. Veja a nota a seguir na tabela.</td> </tr><tr> <td><code>DBI_USER</code><code>mysql_clear_password</code>]</td> <td>O nome de usuário padrão no Windows ao se conectar a<span><strong>mysqld</strong></span>.</td> </tr></tbody></table>

Para obter informações sobre o arquivo de histórico do **mysql**, consulte a Seção 4.5.1.3, “Registro do cliente do mysql”.

`MYSQL_TEST_LOGIN_FILE` é o nome do caminho do arquivo de login (o arquivo criado pelo **mysql_config_editor**). Se não for definido, o valor padrão é o diretório `%APPDATA%\MySQL\.mylogin.cnf` no Windows e `$HOME/.mylogin.cnf` em sistemas que não são do Windows. Veja a Seção 4.6.6, “mysql_config_editor — Ferramenta de Configuração do MySQL”.

As variáveis `MYSQL_TEST_TRACE_DEBUG` e `MYSQL_TEST_TRACE_CRASH` controlam o plugin de cliente de registro do protocolo de teste, se o MySQL for compilado com esse plugin habilitado. Para obter mais informações, consulte o uso do plugin de registro do protocolo de teste.

Os valores padrão de `UMASK` e `UMASK_DIR` são `0640` e `0750`, respectivamente. O MySQL assume que o valor de `UMASK` ou `UMASK_DIR` está em octal se começar com um zero. Por exemplo, definir `UMASK=0600` é equivalente a `UMASK=384`, pois 0600 octal é 384 decimal.

As variáveis `UMASK` e `UMASK_DIR`, apesar de seus nomes, são usadas como modos, não como máscaras:

- Se `UMASK` estiver definido, o **mysqld** usa `($UMASK | 0600)` como o modo para a criação de arquivos, de modo que os arquivos recém-criados tenham um modo na faixa de 0600 a 0666 (todos os valores octal).

- Se `UMASK_DIR` estiver definido, o **mysqld** usa `($UMASK_DIR | 0700)` como o modo base para a criação de diretórios, que é então e-bitado com `~(~$UMASK & 0666)`, para que os diretórios recém-criados tenham um modo na faixa de 0700 a 0777 (todos os valores octal). A operação E pode remover permissões de leitura e escrita do modo do diretório, mas não as de execução.

Veja também a Seção B.3.3.1, “Problemas com permissões de arquivo”.

Pode ser necessário definir `PKG_CONFIG_PATH` se você usar **pkg-config** para construir programas do MySQL. Veja Construindo programas de clientes de API C usando pkg-config.
