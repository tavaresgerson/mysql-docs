#### 13.6.5.3. Declaração ITERATE

```sql
ITERATE label
```

`ITERATE` pode aparecer apenas dentro das instruções `LOOP`, `REPEAT` e `WHILE`. `ITERATE` significa “começar o loop novamente.”

Para um exemplo, veja Seção 13.6.5.5, “Instrução LOOP”.
