#### 26.4.3.33 As Views session e x$session

Essas views são similares a `processlist` e `x$processlist`, mas elas filtram os background processes para exibir apenas as user sessions. Para descrições das colunas, veja a Seção 26.4.3.22, “As Views processlist e x$processlist”.