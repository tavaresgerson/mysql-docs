## 12.2 Referência de Função Carregável

A tabela a seguir lista cada função que pode ser carregada dinamicamente e fornece uma breve descrição de cada uma. Para uma tabela que lista funções e operadores integrados, consulte a Seção 12.1, “Referência de Funções e Operadores Integrados”.

Para obter informações gerais sobre funções carregáveis, consulte a Seção 5.6, “Funções carregáveis do MySQL Server”.

**Tabela 12.2 Funções carregáveis**

<table frame="box" rules="all" summary="Uma referência que lista todas as funções carregáveis."><col style="width: 28%"/><col style="width: 71%"/><thead><tr><th>Nome</th> <th>Descrição</th> </tr></thead><tbody><tr><td>[[PH_HTML_CODE_<code>audit_log_filter_set_filter()</code>]</td> <td>Descifre o texto cifrado usando uma chave privada ou pública</td> </tr><tr><td>[[PH_HTML_CODE_<code>audit_log_filter_set_filter()</code>]</td> <td>Derivar chave simétrica a partir de chaves assimétricas</td> </tr><tr><td>[[PH_HTML_CODE_<code>audit_log_read()</code>]</td> <td>Criptografar texto em claro usando chave privada ou pública</td> </tr><tr><td>[[PH_HTML_CODE_<code>audit_log_read_bookmark()</code>]</td> <td>Gerar assinatura a partir do digest</td> </tr><tr><td>[[PH_HTML_CODE_<code>create_asymmetric_priv_key()</code>]</td> <td>Verifique se a assinatura corresponde ao digest</td> </tr><tr><td>[[PH_HTML_CODE_<code>create_asymmetric_pub_key()</code>]</td> <td>Obter a senha de criptografia do log de auditoria</td> </tr><tr><td>[[PH_HTML_CODE_<code>create_dh_parameters()</code>]</td> <td>Definir senha de criptografia do log de auditoria</td> </tr><tr><td>[[PH_HTML_CODE_<code>create_digest()</code>]</td> <td>Limpar as tabelas do filtro do log de auditoria</td> </tr><tr><td>[[PH_HTML_CODE_<code>flush_rewrite_rules()</code>]</td> <td>Remover filtro do log de auditoria</td> </tr><tr><td>[[PH_HTML_CODE_<code>gen_blacklist()</code>]</td> <td>Desvincule o filtro do registro de auditoria do usuário</td> </tr><tr><td>[[<code>audit_log_filter_set_filter()</code>]]</td> <td>Definir filtro de registro de auditoria</td> </tr><tr><td>[[<code>asymmetric_derive()</code><code>audit_log_filter_set_filter()</code>]</td> <td>Atribuir filtro de registro de auditoria ao usuário</td> </tr><tr><td>[[<code>audit_log_read()</code>]]</td> <td>Registros do log de auditoria de retorno</td> </tr><tr><td>[[<code>audit_log_read_bookmark()</code>]]</td> <td>Marcar como favorito o evento mais recente do registro de auditoria</td> </tr><tr><td>[[<code>create_asymmetric_priv_key()</code>]]</td> <td>Criar chave privada</td> </tr><tr><td>[[<code>create_asymmetric_pub_key()</code>]]</td> <td>Criar chave pública</td> </tr><tr><td>[[<code>create_dh_parameters()</code>]]</td> <td>Gerar segredo de DH compartilhado</td> </tr><tr><td>[[<code>create_digest()</code>]]</td> <td>Gerar resumo a partir da string</td> </tr><tr><td>[[<code>flush_rewrite_rules()</code>]]</td> <td>Carregue a tabela rewrite_rules no cache do Rewriter</td> </tr><tr><td>[[<code>gen_blacklist()</code>]]</td> <td>Realizar a substituição de termos do dicionário</td> </tr><tr><td>[[<code>asymmetric_encrypt()</code><code>audit_log_filter_set_filter()</code>]</td> <td>Remover o dicionário do registro</td> </tr><tr><td>[[<code>asymmetric_encrypt()</code><code>audit_log_filter_set_filter()</code>]</td> <td>Carregar o dicionário no registro</td> </tr><tr><td>[[<code>asymmetric_encrypt()</code><code>audit_log_read()</code>]</td> <td>Retorne um termo aleatório do dicionário</td> </tr><tr><td>[[<code>asymmetric_encrypt()</code><code>audit_log_read_bookmark()</code>]</td> <td>Gerar número aleatório dentro do intervalo</td> </tr><tr><td>[[<code>asymmetric_encrypt()</code><code>create_asymmetric_priv_key()</code>]</td> <td>Gerar endereço de e-mail aleatório</td> </tr><tr><td>[[<code>asymmetric_encrypt()</code><code>create_asymmetric_pub_key()</code>]</td> <td>Gerar número de conta principal aleatório de cartão de pagamento</td> </tr><tr><td>[[<code>asymmetric_encrypt()</code><code>create_dh_parameters()</code>]</td> <td>Gerar um Número de Segurança Social aleatório dos EUA</td> </tr><tr><td>[[<code>asymmetric_encrypt()</code><code>create_digest()</code>]</td> <td>Gerar número de telefone aleatório dos EUA</td> </tr><tr><td>[[<code>asymmetric_encrypt()</code><code>flush_rewrite_rules()</code>]</td> <td>Rotacionar a chave mestre do cliente da AWS</td> </tr><tr><td>[[<code>asymmetric_encrypt()</code><code>gen_blacklist()</code>]</td> <td>Rotacionar chaves no arquivo de armazenamento keyring_aws</td> </tr><tr><td>[[<code>asymmetric_sign()</code><code>audit_log_filter_set_filter()</code>]</td> <td>Obter o valor da chave do chaveiro</td> </tr><tr><td>[[<code>asymmetric_sign()</code><code>audit_log_filter_set_filter()</code>]</td> <td>Gerar chave aleatória para chaveiro</td> </tr><tr><td>[[<code>asymmetric_sign()</code><code>audit_log_read()</code>]</td> <td>Comprimento da chave do chaveiro de retorno</td> </tr><tr><td>[[<code>asymmetric_sign()</code><code>audit_log_read_bookmark()</code>]</td> <td>Remova a chave do chaveiro</td> </tr><tr><td>[[<code>asymmetric_sign()</code><code>create_asymmetric_priv_key()</code>]</td> <td>Guarde a chave no chaveiro</td> </tr><tr><td>[[<code>asymmetric_sign()</code><code>create_asymmetric_pub_key()</code>]</td> <td>Tipo de chave do chaveiro de retorno</td> </tr><tr><td>[[<code>asymmetric_sign()</code><code>create_dh_parameters()</code>]</td> <td>Roteiro de auxílio ao plugin de reescritor</td> </tr><tr><td>[[<code>asymmetric_sign()</code><code>create_digest()</code>]</td> <td>Parte interna da máscara da corda</td> </tr><tr><td>[[<code>asymmetric_sign()</code><code>flush_rewrite_rules()</code>]</td> <td>Máscara esquerda e direita da corda</td> </tr><tr><td>[[<code>asymmetric_sign()</code><code>gen_blacklist()</code>]</td> <td>Pagamento do cartão de pagamento Número da Conta Principal parte da string</td> </tr><tr><td>[[<code>asymmetric_verify()</code><code>audit_log_filter_set_filter()</code>]</td> <td>Pagamento do cartão de pagamento Número da Conta Principal parte da string</td> </tr><tr><td>[[<code>asymmetric_verify()</code><code>audit_log_filter_set_filter()</code>]</td> <td>Mascarar o Número de Segurança Social dos EUA</td> </tr><tr><td>[[<code>asymmetric_verify()</code><code>audit_log_read()</code>]</td> <td>Reinicie as variáveis de status do firewall</td> </tr><tr><td>[[<code>asymmetric_verify()</code><code>audit_log_read_bookmark()</code>]</td> <td>Normalize a declaração SQL para o formato de digestão</td> </tr><tr><td>[[<code>asymmetric_verify()</code><code>create_asymmetric_priv_key()</code>]</td> <td>Atualizar o cache do perfil da conta do firewall</td> </tr><tr><td>[[<code>asymmetric_verify()</code><code>create_asymmetric_pub_key()</code>]</td> <td>Atualize o perfil da conta do firewall cache de registro de declarações</td> </tr><tr><td>[[<code>asymmetric_verify()</code><code>create_dh_parameters()</code>]</td> <td>Adquira serviço de bloqueio de fechaduras compartilhadas</td> </tr><tr><td>[[<code>asymmetric_verify()</code><code>create_digest()</code>]</td> <td>Adquira fechaduras de serviço de bloqueio exclusivas</td> </tr><tr><td>[[<code>asymmetric_verify()</code><code>flush_rewrite_rules()</code>]</td> <td>Serviços de liberação de bloqueios de cadeados</td> </tr><tr><td>[[<code>asymmetric_verify()</code><code>gen_blacklist()</code>]</td> <td>Estabelecer o modo operacional do perfil da conta do firewall</td> </tr><tr><td>[[<code>audit_log_encryption_password_get()</code><code>audit_log_filter_set_filter()</code>]</td> <td>Excluir tokens da lista de tokens de versão</td> </tr><tr><td>[[<code>audit_log_encryption_password_get()</code><code>audit_log_filter_set_filter()</code>]</td> <td>Modificar a lista de tokens de versão</td> </tr><tr><td>[[<code>audit_log_encryption_password_get()</code><code>audit_log_read()</code>]</td> <td>Adquira bloqueios exclusivos nos tokens da versão</td> </tr><tr><td>[[<code>audit_log_encryption_password_get()</code><code>audit_log_read_bookmark()</code>]</td> <td>Adquirir bloqueios compartilhados em tokens de versão</td> </tr><tr><td>[[<code>audit_log_encryption_password_get()</code><code>create_asymmetric_priv_key()</code>]</td> <td>Lista de tokens de versão definida</td> </tr><tr><td>[[<code>audit_log_encryption_password_get()</code><code>create_asymmetric_pub_key()</code>]</td> <td>Lista de tokens de versão de retorno</td> </tr><tr><td>[[<code>audit_log_encryption_password_get()</code><code>create_dh_parameters()</code>]</td> <td>Tokens de versão de liberação de bloqueios</td> </tr></tbody></table>
