## 12.2 Referência de Funções Carregáveis

A tabela a seguir lista cada função que pode ser carregada em tempo de execução e fornece uma breve descrição de cada uma. Para uma tabela listando funções e operadores integrados, consulte a Seção 12.1, “Referência de Funções e Operadores Integrados”.

Para informações gerais sobre funções carregáveis, consulte a Seção 5.6, “Funções Carregáveis do Servidor MySQL”.

**Tabela 12.2 Funções Carregáveis**

<table frame="box" rules="all" summary="Uma referência que lista todas as funções carregáveis."><col style="width: 28%"/><col style="width: 71%"/><thead><tr><th>Nome</th> <th>Descrição</th> </tr></thead><tbody><tr><td><code>asymmetric_decrypt()</code></td> <td> Descriptografa ciphertext usando private ou public key </td> </tr><tr><td><code>asymmetric_derive()</code></td> <td> Deriva symmetric key de asymmetric keys </td> </tr><tr><td><code>asymmetric_encrypt()</code></td> <td> Criptografa cleartext usando private ou public key </td> </tr><tr><td><code>asymmetric_sign()</code></td> <td> Gera signature a partir do digest </td> </tr><tr><td><code>asymmetric_verify()</code></td> <td> Verifica se signature corresponde ao digest </td> </tr><tr><td><code>audit_log_encryption_password_get()</code></td> <td> Busca a password de criptografia do audit log </td> </tr><tr><td><code>audit_log_encryption_password_set()</code></td> <td> Define a password de criptografia do audit log </td> </tr><tr><td><code>audit_log_filter_flush()</code></td> <td> Esvazia (Flush) as tabelas de filter do audit log </td> </tr><tr><td><code>audit_log_filter_remove_filter()</code></td> <td> Remove o filter do audit log </td> </tr><tr><td><code>audit_log_filter_remove_user()</code></td> <td> Desvincula o filter do audit log do user </td> </tr><tr><td><code>audit_log_filter_set_filter()</code></td> <td> Define o filter do audit log </td> </tr><tr><td><code>audit_log_filter_set_user()</code></td> <td> Atribui o filter do audit log ao user </td> </tr><tr><td><code>audit_log_read()</code></td> <td> Retorna os records do audit log </td> </tr><tr><td><code>audit_log_read_bookmark()</code></td> <td> Bookmark para o evento mais recente do audit log </td> </tr><tr><td><code>create_asymmetric_priv_key()</code></td> <td> Cria private key </td> </tr><tr><td><code>create_asymmetric_pub_key()</code></td> <td> Cria public key </td> </tr><tr><td><code>create_dh_parameters()</code></td> <td> Gera shared DH secret </td> </tr><tr><td><code>create_digest()</code></td> <td> Gera digest a partir de uma string </td> </tr><tr><td><code>flush_rewrite_rules()</code></td> <td> Carrega a tabela `rewrite_rules` no cache do Rewriter </td> </tr><tr><td><code>gen_blacklist()</code></td> <td> Realiza a substituição de termos do dictionary </td> </tr><tr><td><code>gen_dictionary_drop()</code></td> <td> Remove o dictionary do registry </td> </tr><tr><td><code>gen_dictionary_load()</code></td> <td> Carrega o dictionary no registry </td> </tr><tr><td><code>gen_dictionary()</code></td> <td> Retorna um termo aleatório do dictionary </td> </tr><tr><td><code>gen_range()</code></td> <td> Gera número aleatório dentro do range </td> </tr><tr><td><code>gen_rnd_email()</code></td> <td> Gera endereço de email aleatório </td> </tr><tr><td><code>gen_rnd_pan()</code></td> <td> Gera Primary Account Number (PAN) aleatório de cartão de pagamento </td> </tr><tr><td><code>gen_rnd_ssn()</code></td> <td> Gera US Social Security Number aleatório </td> </tr><tr><td><code>gen_rnd_us_phone()</code></td> <td> Gera número de telefone dos EUA aleatório </td> </tr><tr><td><code>keyring_aws_rotate_cmk()</code></td> <td> Rotação da customer master key (CMK) do AWS </td> </tr><tr><td><code>keyring_aws_rotate_keys()</code></td> <td> Rotação das keys no arquivo de storage `keyring_aws` </td> </tr><tr><td><code>keyring_key_fetch()</code></td> <td> Busca o value da keyring key </td> </tr><tr><td><code>keyring_key_generate()</code></td> <td> Gera keyring key aleatória </td> </tr><tr><td><code>keyring_key_length_fetch()</code></td> <td> Retorna o length da keyring key </td> </tr><tr><td><code>keyring_key_remove()</code></td> <td> Remove a keyring key </td> </tr><tr><td><code>keyring_key_store()</code></td> <td> Armazena a key no keyring </td> </tr><tr><td><code>keyring_key_type_fetch()</code></td> <td> Retorna o type da keyring key </td> </tr><tr><td><code>load_rewrite_rules()</code></td> <td> Rotina de auxílio do Rewriter plugin </td> </tr><tr><td><code>mask_inner()</code></td> <td> Mascara a parte interior de uma string </td> </tr><tr><td><code>mask_outer()</code></td> <td> Mascara as partes esquerda e direita de uma string </td> </tr><tr><td><code>mask_pan()</code></td> <td> Mascara a parte Primary Account Number (PAN) de cartão de pagamento de uma string </td> </tr><tr><td><code>mask_pan_relaxed()</code></td> <td> Mascara a parte Primary Account Number (PAN) de cartão de pagamento de uma string </td> </tr><tr><td><code>mask_ssn()</code></td> <td> Mascara US Social Security Number </td> </tr><tr><td><code>mysql_firewall_flush_status()</code></td> <td> Reseta as variáveis de status do firewall </td> </tr><tr><td><code>normalize_statement()</code></td> <td> Normaliza o SQL statement para a forma de digest </td> </tr><tr><td><code>read_firewall_users()</code></td> <td> Atualiza o cache de account profile do firewall </td> </tr><tr><td><code>read_firewall_whitelist()</code></td> <td> Atualiza o cache de recorded-statement do account profile do firewall </td> </tr><tr><td><code>service_get_read_locks()</code></td> <td> Adquire shared locks do locking service </td> </tr><tr><td><code>service_get_write_locks()</code></td> <td> Adquire exclusive locks do locking service </td> </tr><tr><td><code>service_release_locks()</code></td> <td> Libera os locks do locking service </td> </tr><tr><td><code>set_firewall_mode()</code></td> <td> Estabelece o operational mode do account profile do firewall </td> </tr><tr><td><code>version_tokens_delete()</code></td> <td> Deleta tokens da version tokens list </td> </tr><tr><td><code>version_tokens_edit()</code></td> <td> Modifica a version tokens list </td> </tr><tr><td><code>version_tokens_lock_exclusive()</code></td> <td> Adquire exclusive locks nas version tokens </td> </tr><tr><td><code>version_tokens_lock_shared()</code></td> <td> Adquire shared locks nas version tokens </td> </tr><tr><td><code>version_tokens_set()</code></td> <td> Define a version tokens list </td> </tr><tr><td><code>version_tokens_show()</code></td> <td> Retorna a version tokens list </td> </tr><tr><td><code>version_tokens_unlock()</code></td> <td> Libera os locks das version tokens </td> </tr></tbody></table>