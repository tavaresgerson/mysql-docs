## 12.6 Funções e operadores numéricos

12.6.1 Operadores aritméticos

12.6.2 Funções matemáticas

**Tabela 12.8 Funções e Operadores Numéricos**

<table frame="box" rules="all" summary="Uma referência que lista funções e operadores numéricos."><col style="width: 28%"/><col style="width: 71%"/><thead><tr><th>Nome</th> <th>Descrição</th> </tr></thead><tbody><tr><td>[[PH_HTML_CODE_<code>ATAN()</code>], [[PH_HTML_CODE_<code>ATAN()</code>]</td> <td>Operador módulo</td> </tr><tr><td>[[PH_HTML_CODE_<code>ATAN()</code>]</td> <td>Operador de multiplicação</td> </tr><tr><td>[[PH_HTML_CODE_<code>CEIL()</code>]</td> <td>Operador de adição</td> </tr><tr><td>[[PH_HTML_CODE_<code>CEILING()</code>]</td> <td>Operador de menos</td> </tr><tr><td>[[PH_HTML_CODE_<code>CONV()</code>]</td> <td>Troque o sinal do argumento</td> </tr><tr><td>[[PH_HTML_CODE_<code>COS()</code>]</td> <td>Operador de divisão</td> </tr><tr><td>[[PH_HTML_CODE_<code>COT()</code>]</td> <td>Retorne o valor absoluto</td> </tr><tr><td>[[PH_HTML_CODE_<code>CRC32()</code>]</td> <td>Retorne o cosseno do arco</td> </tr><tr><td>[[PH_HTML_CODE_<code>DEGREES()</code>]</td> <td>Retorne o arco seno</td> </tr><tr><td>[[<code>ATAN()</code>]]</td> <td>Retorne a tangente do arco</td> </tr><tr><td>[[<code>MOD</code><code>ATAN()</code>], [[<code>ATAN()</code>]]</td> <td>Retorne o arco tangente dos dois argumentos</td> </tr><tr><td>[[<code>CEIL()</code>]]</td> <td>Retorne o menor valor inteiro não menor que o argumento</td> </tr><tr><td>[[<code>CEILING()</code>]]</td> <td>Retorne o menor valor inteiro não menor que o argumento</td> </tr><tr><td>[[<code>CONV()</code>]]</td> <td>Converter números entre diferentes bases numéricas</td> </tr><tr><td>[[<code>COS()</code>]]</td> <td>Retorne o cosseno</td> </tr><tr><td>[[<code>COT()</code>]]</td> <td>Retorne a co-tangente</td> </tr><tr><td>[[<code>CRC32()</code>]]</td> <td>Calcule o valor de verificação de redundância cíclica</td> </tr><tr><td>[[<code>DEGREES()</code>]]</td> <td>Converter radianos em graus</td> </tr><tr><td>[[<code>*</code><code>ATAN()</code>]</td> <td>Divisão inteira</td> </tr><tr><td>[[<code>*</code><code>ATAN()</code>]</td> <td>Eleve à potência de</td> </tr><tr><td>[[<code>*</code><code>ATAN()</code>]</td> <td>Retorne o maior valor inteiro que não seja maior que o argumento</td> </tr><tr><td>[[<code>*</code><code>CEIL()</code>]</td> <td>Retorne o logaritmo natural do argumento</td> </tr><tr><td>[[<code>*</code><code>CEILING()</code>]</td> <td>Retorne o logaritmo natural do primeiro argumento</td> </tr><tr><td>[[<code>*</code><code>CONV()</code>]</td> <td>Retorne o logaritmo decimal do argumento</td> </tr><tr><td>[[<code>*</code><code>COS()</code>]</td> <td>Retorne o logaritmo em base 2 do argumento</td> </tr><tr><td>[[<code>*</code><code>COT()</code>]</td> <td>Devolva o restante</td> </tr><tr><td>[[<code>*</code><code>CRC32()</code>]</td> <td>Retorne o valor de pi</td> </tr><tr><td>[[<code>*</code><code>DEGREES()</code>]</td> <td>Retorne o argumento elevado à potência especificada</td> </tr><tr><td>[[<code>+</code><code>ATAN()</code>]</td> <td>Retorne o argumento elevado à potência especificada</td> </tr><tr><td>[[<code>+</code><code>ATAN()</code>]</td> <td>Argumento de retorno convertido para radianos</td> </tr><tr><td>[[<code>+</code><code>ATAN()</code>]</td> <td>Retorne um valor aleatório de ponto flutuante</td> </tr><tr><td>[[<code>+</code><code>CEIL()</code>]</td> <td>Em torno do argumento</td> </tr><tr><td>[[<code>+</code><code>CEILING()</code>]</td> <td>Retorne o sinal do argumento</td> </tr><tr><td>[[<code>+</code><code>CONV()</code>]</td> <td>Retorne o seno do argumento</td> </tr><tr><td>[[<code>+</code><code>COS()</code>]</td> <td>Retorne a raiz quadrada do argumento</td> </tr><tr><td>[[<code>+</code><code>COT()</code>]</td> <td>Retorne a tangente do argumento</td> </tr><tr><td>[[<code>+</code><code>CRC32()</code>]</td> <td>Reduzir para o número especificado de casas decimais</td> </tr></tbody></table>
