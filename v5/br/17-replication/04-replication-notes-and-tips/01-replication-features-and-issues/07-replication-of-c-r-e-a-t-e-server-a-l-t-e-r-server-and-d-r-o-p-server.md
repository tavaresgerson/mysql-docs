#### 16.4.1.7 Replicação de CREATE SERVER, ALTER SERVER e DROP SERVER

As *statements* `CREATE SERVER`, `ALTER SERVER` e `DROP SERVER` não são escritas no *binary log*, independentemente do *binary logging format* que esteja em uso.