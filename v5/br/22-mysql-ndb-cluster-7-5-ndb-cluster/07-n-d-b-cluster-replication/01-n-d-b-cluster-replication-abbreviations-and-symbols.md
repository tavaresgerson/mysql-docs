### 21.7.1 Replicação em cluster do NDB: Abreviações e Símbolos

Ao longo desta seção, usaremos as seguintes abreviações ou símbolos para referir-se aos clusters de origem e replicação, bem como aos processos e comandos executados nos clusters ou nós do cluster:

**Tabela 21.63 Abreviações utilizadas ao longo desta seção, referindo-se a clusters de origem e replicação, e a processos e comandos executados nos nós do cluster**

<table><thead><tr> <th>Símbolo ou Abreviação</th> <th>Descrição (Refere-se a...)</th> </tr></thead><tbody><tr> <td><em class="replaceable">[[PH_HTML_CODE_<code>S'</code>]</em></td> <td>O grupo que atua como a fonte de replicação (primaria)</td> </tr><tr> <td><em class="replaceable">[[PH_HTML_CODE_<code>S'</code>]</em></td> <td>O grupo que atua como a (replica) primária</td> </tr><tr> <td>[[<code>shell<em class="replaceable"><code>S</code>]]</em>&gt;</code></td> <td>Comando de shell a ser emitido no cluster de origem</td> </tr><tr> <td>[[<code>mysql<em class="replaceable"><code>S</code>]]</em>&gt;</code></td> <td>Comando do cliente MySQL emitido em um único servidor MySQL que está rodando como um nó SQL no clúster de origem</td> </tr><tr> <td>[[<code>mysql<em class="replaceable"><code>S*</code>]]</em>&gt;</code></td> <td>Comando do cliente MySQL a ser emitido em todos os nós SQL que participam do clúster de origem de replicação</td> </tr><tr> <td>[[<code>shell<em class="replaceable"><code>R</code>]]</em>&gt;</code></td> <td>Comando de shell a ser emitido no cluster replica</td> </tr><tr> <td>[[<code>mysql<em class="replaceable"><code>R</code>]]</em>&gt;</code></td> <td>Comando do cliente MySQL emitido em um único servidor MySQL que está rodando como um nó SQL no clúster de replica</td> </tr><tr> <td>[[<code>mysql<em class="replaceable"><code>R*</code>]]</em>&gt;</code></td> <td>Comando do cliente MySQL a ser emitido em todos os nós SQL que participam do clúster de replica</td> </tr><tr> <td><em class="replaceable">[[<code>C</code>]]</em></td> <td>Canal de replicação primário</td> </tr><tr> <td><em class="replaceable">[[<code>C'</code>]]</em></td> <td>Canal de replicação secundário</td> </tr><tr> <td><em class="replaceable">[[<code>S'</code>]]</em></td> <td>Fonte de replicação secundária</td> </tr><tr> <td><em class="replaceable">[[<code>R</code><code>S'</code>]</em></td> <td>Replica secundária</td> </tr></tbody></table>
