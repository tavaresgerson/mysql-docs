## 6.9 Variáveis de Ambiente

Esta seção lista as variáveis de ambiente que são usadas diretamente ou indiretamente pelo MySQL. A maioria dessas variáveis também pode ser encontrada em outros lugares deste manual.

As opções na linha de comando têm precedência sobre os valores especificados em arquivos de opção e variáveis de ambiente, e os valores nos arquivos de opção têm precedência sobre os valores nas variáveis de ambiente. Em muitos casos, é preferível usar um arquivo de opção em vez de variáveis de ambiente para modificar o comportamento do MySQL. Veja a Seção 6.2.2.2, “Usando arquivos de opção”.

<table summary="Environment variables that are used directly or indirectly by MySQL."><col style="width: 40%"/><col style="width: 60%"/><thead><tr> <th>Variable</th> <th>Descrição</th> </tr></thead><tbody><tr> <td><code>AUTHENTICATION_KERBEROS_CLIENT_LOG</code></td> <td>Nível de registro de autenticação Kerberos.</td> </tr><tr> <td><code>AUTHENTICATION_LDAP_CLIENT_LOG</code></td> <td>Nível de registro de autenticação LDAP do lado do cliente.</td> </tr><tr> <td><code>AUTHENTICATION_PAM_LOG</code></td> <td>Configurações de registro de depuração do plugin de autenticação PAM.</td> </tr><tr> <td><code>CC</code></td> <td>O nome do seu compilador C (para executar o CMake).</td> </tr><tr> <td><code>CXX</code></td> <td>O nome do seu compilador C++ (para executar o CMake).</td> </tr><tr> <td><code>CC</code></td> <td>O nome do seu compilador C (para executar o CMake).</td> </tr><tr> <td><code>DBI_USER</code></td> <td>O nome de usuário padrão para Perl DBI.</td> </tr><tr> <td><code>DBI_TRACE</code></td> <td>Opções de rastreamento para Perl DBI.</td> </tr><tr> <td><code>HOME</code></td> <td>O caminho padrão para o arquivo de histórico do mysql é<code>$HOME/.mysql_history</code>.</td> </tr><tr> <td><code>LD_RUN_PATH</code></td> <td>Usado para especificar a localização de<code>libmysqlclient.so</code>.</td> </tr><tr> <td><code>LIBMYSQL_ENABLE_CLEARTEXT_PLUGIN</code></td> <td>Ativar<code>mysql_clear_password</code>plugin de autenticação; consulte a Seção 8.4.1.4, “Autenticação Cleartext Pluggable do Lado do Cliente”.</td> </tr><tr> <td><code>LIBMYSQL_PLUGIN_DIR</code></td> <td>Diretório onde procurar plugins do cliente.</td> </tr><tr> <td><code>LIBMYSQL_PLUGINS</code></td> <td>Plugins do cliente para pré-carregar.</td> </tr><tr> <td><code>MYSQL_DEBUG</code></td> <td>Opções de rastreamento de depuração durante a depuração.</td> </tr><tr> <td><code>MYSQL_GROUP_SUFFIX</code></td> <td>Valor do sufixo do grupo de opções (como especificar<code>--defaults-group-suffix</code>).</td> </tr><tr> <td><code>MYSQL_HISTFILE</code></td> <td>O caminho para o arquivo de histórico do mysql. Se esta variável for definida, seu valor substitui o padrão para<code>$HOME/.mysql_history</code>.</td> </tr><tr> <td><code>MYSQL_HISTIGNORE</code></td> <td>Padrões que especificam declarações que o mysql não deve registrar<code>$HOME/.mysql_history</code>, ou<code>syslog</code>se<code>--syslog</code>é dada.</td> </tr><tr> <td><code>MYSQL_HOME</code></td> <td>O caminho para o diretório em que o servidor específico está<code>my.cnf</code>o arquivo reside.</td> </tr><tr> <td><code>MYSQL_HOST</code></td> <td>O nome de host padrão usado pelo cliente de linha de comando mysql.</td> </tr><tr> <td><code>MYSQL_OPENSSL_UDF_DH_BITS_THRESHOLD</code></td> <td>Comprimento máximo da chave para<code>create_dh_parameters()</code>Veja a Seção 8.6.3, “Uso e exemplos de criptografia empresarial do MySQL”.</td> </tr><tr> <td><code>MYSQL_OPENSSL_UDF_DSA_BITS_THRESHOLD</code></td> <td>Tamanho máximo da chave DSA para<code>create_asymmetric_priv_key()</code>Veja a Seção 8.6.3, “Uso e exemplos de criptografia empresarial do MySQL”.</td> </tr><tr> <td><code>MYSQL_OPENSSL_UDF_RSA_BITS_THRESHOLD</code></td> <td>Comprimento máximo da chave RSA para<code>create_asymmetric_priv_key()</code>Veja a Seção 8.6.3, “Uso e exemplos de criptografia empresarial do MySQL”.</td> </tr><tr> <td><code>MYSQL_PS1</code></td> <td>O prompt de comando a ser usado no cliente de linha de comando mysql.</td> </tr><tr> <td><code>MYSQL_PWD</code></td> <td>A senha padrão ao se conectar ao mysqld. Usá-la é inseguro. Veja a nota a seguir na tabela.</td> </tr><tr> <td><code>MYSQL_TCP_PORT</code></td> <td>O número de porta TCP/IP padrão.</td> </tr><tr> <td><code>MYSQL_TEST_LOGIN_FILE</code></td> <td>O nome do<code>.mylogin.cnf</code>arquivo de caminho de login.</td> </tr><tr> <td><code>MYSQL_TEST_TRACE_CRASH</code></td> <td>Se o plugin de registro do protocolo de teste causa falhas nos clientes. Veja a nota a seguir à tabela.</td> </tr><tr> <td><code>MYSQL_TEST_TRACE_DEBUG</code></td> <td>Se o plugin de registro do protocolo de teste produz saída. Veja a nota a seguir à tabela.</td> </tr><tr> <td><code>MYSQL_UNIX_PORT</code></td> <td>O nome padrão do arquivo de socket Unix; usado para conexões com<code>localhost</code>.</td> </tr><tr> <td><code>MYSQLX_TCP_PORT</code></td> <td>O número de porta TCP/IP padrão do X Plugin.</td> </tr><tr> <td><code>MYSQLX_UNIX_PORT</code></td> <td>O nome padrão do arquivo de soquete Unix do X Plugin; usado para conexões com<code>localhost</code>.</td> </tr><tr> <td><code>NOTIFY_SOCKET</code></td> <td>Conexão usada pelo mysqld para se comunicar com o systemd.</td> </tr><tr> <td><code>PATH</code></td> <td>Utilizado pelo shell para encontrar programas MySQL.</td> </tr><tr> <td><code>PKG_CONFIG_PATH</code></td> <td>Localização de<code>mysqlclient.pc</code>arquivo pkg-config. Veja a nota após a tabela.</td> </tr><tr> <td><code>TMPDIR</code></td> <td>O diretório no qual os arquivos temporários são criados.</td> </tr><tr> <td><code>TZ</code></td> <td>Isso deve ser ajustado para o seu fuso horário local. Veja a Seção B.3.3.7, “Problemas com o fuso horário”.</td> </tr><tr> <td><code>UMASK</code></td> <td>O modo de criação de arquivos de usuário ao criar arquivos. Veja a nota a seguir na tabela.</td> </tr><tr> <td><code>UMASK_DIR</code></td> <td>O modo de criação de diretório do usuário ao criar diretórios. Veja a nota a seguir na tabela.</td> </tr><tr> <td><code>USER</code></td> <td>O nome de usuário padrão no Windows ao se conectar ao mysqld.</td> </tr></tbody></table>

Para informações sobre o arquivo de histórico do **mysql**, consulte a Seção 6.5.1.3, “Registro do cliente do mysql”.

O uso de `MYSQL_PWD` para especificar uma senha do MySQL deve ser considerado *extremamente inseguro* e não deve ser usado. Algumas versões do **ps** incluem uma opção para exibir o ambiente dos processos em execução. Em alguns sistemas, se você definir `MYSQL_PWD`, sua senha é exposta a qualquer outro usuário que execute o **ps**. Mesmo em sistemas sem essa versão do **ps**, não é prudente assumir que não existem outros métodos pelos quais os usuários possam examinar os ambientes dos processos.

`MYSQL_PWD` é descontinuado a partir do MySQL 8.0; espere que ele seja removido em uma versão futura do MySQL.

`MYSQL_TEST_LOGIN_FILE` é o nome do caminho do arquivo de login (o arquivo criado pelo **mysql_config_editor**). Se não estiver definido, o valor padrão é o diretório `%APPDATA%\MySQL\.mylogin.cnf` no Windows e `$HOME/.mylogin.cnf` em sistemas que não são do Windows. Veja a Seção 6.6.7, “mysql_config_editor — Ferramenta de Configuração do MySQL”.

As variáveis `MYSQL_TEST_TRACE_DEBUG` e `MYSQL_TEST_TRACE_CRASH` controlam o plugin de registro do protocolo de teste do cliente, se o MySQL for construído com esse plugin habilitado. Para mais informações, consulte o uso do plugin de registro do protocolo de teste.

Os valores padrão de `UMASK` e `UMASK_DIR` são, respectivamente, `0640` e `0750`. O MySQL assume que o valor para `UMASK` ou `UMASK_DIR` é octal se começar com um zero. Por exemplo, definir `UMASK=0600` é equivalente a `UMASK=384`, porque 0600 octal é 384 decimal.

As variáveis `UMASK` e `UMASK_DIR`, apesar de seus nomes, são usadas como modos, não como máscaras:

* Se `UMASK` estiver definido, o **mysqld** utiliza `($UMASK | 0600)` como o modo para a criação de arquivos, de modo que os arquivos recém-criados tenham um modo na faixa de 0600 a 0666 (todos os valores octal).

* Se `UMASK_DIR` estiver definido, o **mysqld** usa `($UMASK_DIR | 0700)` como o modo base para a criação de diretórios, que é então AND com `~(~$UMASK & 0666)`, de modo que os diretórios recém-criados tenham um modo na faixa de 0700 a 0777 (todos os valores octal). A operação AND pode remover permissões de leitura e escrita do modo do diretório, mas não de execução.

Veja também a Seção B.3.3.1, “Problemas com permissões de arquivo”.

Pode ser necessário definir `PKG_CONFIG_PATH` se você usa **pkg-config** para a construção de programas MySQL. Veja Construindo programas de cliente de API C usando pkg-config.