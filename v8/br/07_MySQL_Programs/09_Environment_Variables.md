## 6.9 Variáveis de ambiente

Esta seção lista as variáveis de ambiente que são usadas diretamente ou indiretamente pelo MySQL. A maioria dessas variáveis também pode ser encontrada em outros lugares deste manual.

As opções na linha de comando têm precedência sobre os valores especificados em arquivos de opção e variáveis de ambiente, e os valores em arquivos de opção têm precedência sobre os valores em variáveis de ambiente. Em muitos casos, é preferível usar um arquivo de opção em vez de variáveis de ambiente para modificar o comportamento do MySQL. Veja a Seção 6.2.2.2, “Usando Arquivos de Opção”.

<table summary="Variáveis de ambiente que são usadas diretamente ou indiretamente pelo MySQL."><thead><tr> <th>Variável</th> <th>Descrição</th> </tr></thead><tbody><tr> <td>[[PH_HTML_CODE_<code>LD_RUN_PATH</code>]</td> <td>Nível de registro da autenticação Kerberos.</td> </tr><tr> <td>[[PH_HTML_CODE_<code>LD_RUN_PATH</code>]</td> <td>Nível de registro de autenticação LDAP no lado do cliente.</td> </tr><tr> <td>[[PH_HTML_CODE_<code>LIBMYSQL_ENABLE_CLEARTEXT_PLUGIN</code>]</td> <td>Configurações de registro de depuração do plugin de autenticação PAM.</td> </tr><tr> <td>[[PH_HTML_CODE_<code>mysql_clear_password</code>]</td> <td>O nome do seu compilador C (para execução<span><strong>CMake</strong></span>).</td> </tr><tr> <td>[[PH_HTML_CODE_<code>LIBMYSQL_PLUGIN_DIR</code>]</td> <td>O nome do seu compilador C++ (para execução<span><strong>CMake</strong></span>).</td> </tr><tr> <td>[[PH_HTML_CODE_<code>LIBMYSQL_PLUGINS</code>]</td> <td>O nome do seu compilador C (para execução<span><strong>CMake</strong></span>).</td> </tr><tr> <td>[[PH_HTML_CODE_<code>MYSQL_DEBUG</code>]</td> <td>O nome de usuário padrão para Perl DBI.</td> </tr><tr> <td>[[PH_HTML_CODE_<code>MYSQL_GROUP_SUFFIX</code>]</td> <td>Opções de rastreamento para Perl DBI.</td> </tr><tr> <td>[[PH_HTML_CODE_<code>--defaults-group-suffix</code>]</td> <td>O caminho padrão para o<span><strong>mysql</strong></span>o arquivo de histórico é [[PH_HTML_CODE_<code>MYSQL_HISTFILE</code>].</td> </tr><tr> <td>[[<code>LD_RUN_PATH</code>]]</td> <td>Usado para especificar a localização de [[<code>AUTHENTICATION_LDAP_CLIENT_LOG</code><code>LD_RUN_PATH</code>].</td> </tr><tr> <td>[[<code>LIBMYSQL_ENABLE_CLEARTEXT_PLUGIN</code>]]</td> <td>Ative o plugin de autenticação [[<code>mysql_clear_password</code>]]; consulte a Seção 8.4.1.4, “Autenticação Pluggable em Texto Aberto no Lado do Cliente”.</td> </tr><tr> <td>[[<code>LIBMYSQL_PLUGIN_DIR</code>]]</td> <td>Diretório onde procurar plugins do cliente.</td> </tr><tr> <td>[[<code>LIBMYSQL_PLUGINS</code>]]</td> <td>Plugins do cliente para pré-carregar.</td> </tr><tr> <td>[[<code>MYSQL_DEBUG</code>]]</td> <td>Opções de rastreamento de depuração durante a depuração.</td> </tr><tr> <td>[[<code>MYSQL_GROUP_SUFFIX</code>]]</td> <td>Valor do sufixo do grupo de opções (como especificar [[<code>--defaults-group-suffix</code>]]).</td> </tr><tr> <td>[[<code>MYSQL_HISTFILE</code>]]</td> <td>O caminho para o<span><strong>mysql</strong></span>arquivo de histórico. Se essa variável estiver definida, seu valor substitui o padrão para [[<code>AUTHENTICATION_PAM_LOG</code><code>LD_RUN_PATH</code>].</td> </tr><tr> <td>[[<code>AUTHENTICATION_PAM_LOG</code><code>LD_RUN_PATH</code>]</td> <td>Padrões que especificam declarações que<span><strong>mysql</strong></span>não deve fazer login em [[<code>AUTHENTICATION_PAM_LOG</code><code>LIBMYSQL_ENABLE_CLEARTEXT_PLUGIN</code>], ou [[<code>AUTHENTICATION_PAM_LOG</code><code>mysql_clear_password</code>] se [[<code>AUTHENTICATION_PAM_LOG</code><code>LIBMYSQL_PLUGIN_DIR</code>] for fornecido.</td> </tr><tr> <td>[[<code>AUTHENTICATION_PAM_LOG</code><code>LIBMYSQL_PLUGINS</code>]</td> <td>O caminho para o diretório onde o arquivo específico do servidor [[<code>AUTHENTICATION_PAM_LOG</code><code>MYSQL_DEBUG</code>] reside.</td> </tr><tr> <td>[[<code>AUTHENTICATION_PAM_LOG</code><code>MYSQL_GROUP_SUFFIX</code>]</td> <td>O nome de host padrão usado pelo<span><strong>mysql</strong></span>cliente de linha de comando.</td> </tr><tr> <td>[[<code>AUTHENTICATION_PAM_LOG</code><code>--defaults-group-suffix</code>]</td> <td>Comprimento máximo de chave para [[<code>AUTHENTICATION_PAM_LOG</code><code>MYSQL_HISTFILE</code>]. Consulte a Seção 8.6.3, “Uso e exemplos de criptografia da MySQL Enterprise”.</td> </tr><tr> <td>[[<code>CC</code><code>LD_RUN_PATH</code>]</td> <td>Tamanho máximo da chave DSA para [[<code>CC</code><code>LD_RUN_PATH</code>]. Consulte a Seção 8.6.3, “Uso e exemplos de criptografia da MySQL Enterprise”.</td> </tr><tr> <td>[[<code>CC</code><code>LIBMYSQL_ENABLE_CLEARTEXT_PLUGIN</code>]</td> <td>Comprimento máximo da chave RSA para [[<code>CC</code><code>mysql_clear_password</code>]. Consulte a Seção 8.6.3, “Uso e exemplos de criptografia da MySQL Enterprise”.</td> </tr><tr> <td>[[<code>CC</code><code>LIBMYSQL_PLUGIN_DIR</code>]</td> <td>O prompt de comando a ser usado no<span><strong>mysql</strong></span>cliente de linha de comando.</td> </tr><tr> <td>[[<code>CC</code><code>LIBMYSQL_PLUGINS</code>]</td> <td>A senha padrão ao se conectar a<span><strong>mysqld</strong></span>Usar isso é inseguro. Veja a nota a seguir à tabela.</td> </tr><tr> <td>[[<code>CC</code><code>MYSQL_DEBUG</code>]</td> <td>O número de porta TCP/IP padrão.</td> </tr><tr> <td>[[<code>CC</code><code>MYSQL_GROUP_SUFFIX</code>]</td> <td>O nome do arquivo de caminho de login [[<code>CC</code><code>--defaults-group-suffix</code>].</td> </tr><tr> <td>[[<code>CC</code><code>MYSQL_HISTFILE</code>]</td> <td>Se o plugin de registro do protocolo de teste faz com que os clientes parem de funcionar. Veja a nota após a tabela.</td> </tr><tr> <td>[[<code>CXX</code><code>LD_RUN_PATH</code>]</td> <td>Se o plugin de registro do protocolo de teste produz saída. Veja a nota após a tabela.</td> </tr><tr> <td>[[<code>CXX</code><code>LD_RUN_PATH</code>]</td> <td>O nome padrão do arquivo de socket Unix; usado para conexões ao [[<code>CXX</code><code>LIBMYSQL_ENABLE_CLEARTEXT_PLUGIN</code>].</td> </tr><tr> <td>[[<code>CXX</code><code>mysql_clear_password</code>]</td> <td>O número de porta TCP/IP padrão do X Plugin.</td> </tr><tr> <td>[[<code>CXX</code><code>LIBMYSQL_PLUGIN_DIR</code>]</td> <td>O nome padrão do arquivo de soquete Unix do X Plugin; usado para conexões ao [[<code>CXX</code><code>LIBMYSQL_PLUGINS</code>].</td> </tr><tr> <td>[[<code>CXX</code><code>MYSQL_DEBUG</code>]</td> <td>Conector utilizado por<span><strong>mysqld</strong></span>para se comunicar com o systemd.</td> </tr><tr> <td>[[<code>CXX</code><code>MYSQL_GROUP_SUFFIX</code>]</td> <td>Usado pelo shell para encontrar programas MySQL.</td> </tr><tr> <td>[[<code>CXX</code><code>--defaults-group-suffix</code>]</td> <td>Localização de [[<code>CXX</code><code>MYSQL_HISTFILE</code>]<span><strong>pkg-config</strong></span>arquivo. Veja a nota após a tabela.</td> </tr><tr> <td>[[<code>CC</code><code>LD_RUN_PATH</code>]</td> <td>O diretório onde os arquivos temporários são criados.</td> </tr><tr> <td>[[<code>CC</code><code>LD_RUN_PATH</code>]</td> <td>Isso deve ser ajustado para o fuso horário local. Consulte a Seção B.3.3.7, “Problemas com o Fuso Horário”.</td> </tr><tr> <td>[[<code>CC</code><code>LIBMYSQL_ENABLE_CLEARTEXT_PLUGIN</code>]</td> <td>O modo de criação de arquivos de usuário ao criar arquivos. Veja a nota a seguir na tabela.</td> </tr><tr> <td>[[<code>CC</code><code>mysql_clear_password</code>]</td> <td>O modo de criação do diretório do usuário ao criar diretórios. Veja a nota a seguir na tabela.</td> </tr><tr> <td>[[<code>CC</code><code>LIBMYSQL_PLUGIN_DIR</code>]</td> <td>O nome de usuário padrão no Windows ao se conectar a<span><strong>mysqld</strong></span>.</td> </tr></tbody></table>

Para obter informações sobre o arquivo de histórico do **mysql**, consulte a Seção 6.5.1.3, “Registro do cliente do mysql”.

O uso de `MYSQL_PWD` para especificar uma senha do MySQL deve ser considerado *extremamente inseguro* e não deve ser usado. Algumas versões do **ps** incluem uma opção para exibir o ambiente dos processos em execução. Em alguns sistemas, se você definir `MYSQL_PWD`, sua senha fica exposta a qualquer outro usuário que execute o **ps**. Mesmo em sistemas sem essa versão do **ps**, não é prudente assumir que não existem outros métodos pelos quais os usuários possam examinar os ambientes dos processos.

`MYSQL_PWD` está desatualizado a partir do MySQL 8.0; espere-se que ele seja removido em uma versão futura do MySQL.

`MYSQL_TEST_LOGIN_FILE` é o nome do caminho do arquivo de configuração de login (o arquivo criado pelo **mysql\_config\_editor**). Se não estiver definido, o valor padrão é o diretório `%APPDATA%\MySQL\.mylogin.cnf` no Windows e `$HOME/.mylogin.cnf` em sistemas que não são do Windows. Veja a Seção 6.6.7, “mysql\_config\_editor — Ferramenta de Configuração do MySQL”.

As variáveis `MYSQL_TEST_TRACE_DEBUG` e `MYSQL_TEST_TRACE_CRASH` controlam o plugin de registro do protocolo de execução de testes, se o MySQL for construído com esse plugin habilitado. Para obter mais informações, consulte o uso do plugin de registro de protocolo de execução de testes.

Os valores padrão `UMASK` e `UMASK_DIR` são `0640` e `0750`, respectivamente. O MySQL assume que o valor para `UMASK` ou `UMASK_DIR` está em octal se começar com um zero. Por exemplo, definir `UMASK=0600` é equivalente a `UMASK=384`, pois 0600 octal é 384 decimal.

As variáveis `UMASK` e `UMASK_DIR`, apesar de seus nomes, são usadas como modos, não como máscaras:

- Se `UMASK` estiver definido, o **mysqld** usa `($UMASK | 0600)` como o modo para a criação de arquivos, de modo que os arquivos recém-criados tenham um modo na faixa de 0600 a 0666 (todos os valores octal).

- Se `UMASK_DIR` estiver definido, o **mysqld** usa `($UMASK_DIR | 0700)` como o modo base para a criação de diretórios, que é então e-ANDado com `~(~$UMASK & 0666)`, de modo que os diretórios recém-criados tenham um modo na faixa de 0700 a 0777 (todos os valores octal). A operação e-AND pode remover permissões de leitura e escrita do modo do diretório, mas não as de execução.

Veja também a Seção B.3.3.1, “Problemas com permissões de arquivo”.

Pode ser necessário definir `PKG_CONFIG_PATH` se você usar o **pkg-config** para construir programas do MySQL. Veja Construindo programas de clientes de API C usando pkg-config.
