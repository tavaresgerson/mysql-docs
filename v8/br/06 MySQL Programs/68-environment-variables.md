## 6.9 Variaveis ambientais

Esta seção lista variáveis de ambiente que são usadas direta ou indiretamente pelo MySQL.

As opções na linha de comando têm precedência sobre os valores especificados nos arquivos de opções e nas variáveis de ambiente, e os valores nos arquivos de opções têm precedência sobre os valores nas variáveis de ambiente.

<table><col style="width: 40%"/><col style="width: 60%"/><thead><tr> <th>Variavel</th> <th>Descrição</th> </tr></thead><tbody><tr> <td>[[PH_HTML_CODE_<code>LD_RUN_PATH</code>]</td> <td>Nível de registo de autenticação Kerberos.</td> </tr><tr> <td>[[PH_HTML_CODE_<code>LD_RUN_PATH</code>]</td> <td>Nível de registo de autenticação LDAP do lado do cliente.</td> </tr><tr> <td>[[PH_HTML_CODE_<code>LIBMYSQL_ENABLE_CLEARTEXT_PLUGIN</code>]</td> <td>Configurações de registro de depuração do plugin de autenticação PAM.</td> </tr><tr> <td>[[PH_HTML_CODE_<code>mysql_clear_password</code>]</td> <td>O nome do seu compilador C (para executar<span><strong>Fabricação</strong></span>)</td> </tr><tr> <td>[[PH_HTML_CODE_<code>LIBMYSQL_PLUGIN_DIR</code>]</td> <td>O nome do seu compilador C++ (para executar<span><strong>Fabricação</strong></span>)</td> </tr><tr> <td>[[PH_HTML_CODE_<code>LIBMYSQL_PLUGINS</code>]</td> <td>O nome do seu compilador C (para executar<span><strong>Fabricação</strong></span>)</td> </tr><tr> <td>[[PH_HTML_CODE_<code>MYSQL_DEBUG</code>]</td> <td>O nome de utilizador padrão para o Perl DBI.</td> </tr><tr> <td>[[PH_HTML_CODE_<code>MYSQL_GROUP_SUFFIX</code>]</td> <td>Opções de rastreamento para Perl DBI.</td> </tr><tr> <td>[[PH_HTML_CODE_<code class="option">--defaults-group-suffix</code>]</td> <td>O caminho padrão para o<span><strong>MySQL</strong></span>arquivo de histórico é [[PH_HTML_CODE_<code>MYSQL_HISTFILE</code>].</td> </tr><tr> <td>[[<code>LD_RUN_PATH</code>]]</td> <td>Utilizado para especificar a localização de [[<code>AUTHENTICATION_LDAP_CLIENT_LOG</code><code>LD_RUN_PATH</code>].</td> </tr><tr> <td>[[<code>LIBMYSQL_ENABLE_CLEARTEXT_PLUGIN</code>]]</td> <td>Ativar o plug-in de autenticação [[<code>mysql_clear_password</code>]]; ver Seção 8.4.1.4, Client-Side Cleartext Pluggable Authentication.</td> </tr><tr> <td>[[<code>LIBMYSQL_PLUGIN_DIR</code>]]</td> <td>Diretório no qual procurar plugins de cliente.</td> </tr><tr> <td>[[<code>LIBMYSQL_PLUGINS</code>]]</td> <td>Plugins do cliente para pré-carregar.</td> </tr><tr> <td>[[<code>MYSQL_DEBUG</code>]]</td> <td>Opções de rastreamento de depuração ao depurar.</td> </tr><tr> <td>[[<code>MYSQL_GROUP_SUFFIX</code>]]</td> <td>Valor do sufixo do grupo de opções (como especificar [[<code class="option">--defaults-group-suffix</code>]]).</td> </tr><tr> <td>[[<code>MYSQL_HISTFILE</code>]]</td> <td>O caminho para o<span><strong>MySQL</strong></span>arquivo de histórico. Se esta variável for definida, seu valor substituirá o padrão para [[<code>AUTHENTICATION_PAM_LOG</code><code>LD_RUN_PATH</code>].</td> </tr><tr> <td>[[<code>AUTHENTICATION_PAM_LOG</code><code>LD_RUN_PATH</code>]</td> <td>Padrões que especificam declarações que<span><strong>MySQL</strong></span>não deve fazer login em [[<code>AUTHENTICATION_PAM_LOG</code><code>LIBMYSQL_ENABLE_CLEARTEXT_PLUGIN</code>], ou [[<code>AUTHENTICATION_PAM_LOG</code><code>mysql_clear_password</code>] se [[<code>AUTHENTICATION_PAM_LOG</code><code>LIBMYSQL_PLUGIN_DIR</code>] for fornecido.</td> </tr><tr> <td>[[<code>AUTHENTICATION_PAM_LOG</code><code>LIBMYSQL_PLUGINS</code>]</td> <td>O caminho para o diretório no qual o arquivo [[<code>AUTHENTICATION_PAM_LOG</code><code>MYSQL_DEBUG</code>] específico do servidor reside.</td> </tr><tr> <td>[[<code>AUTHENTICATION_PAM_LOG</code><code>MYSQL_GROUP_SUFFIX</code>]</td> <td>O nome de host padrão usado pelo<span><strong>MySQL</strong></span>Cliente de linha de comando.</td> </tr><tr> <td>[[<code>AUTHENTICATION_PAM_LOG</code><code class="option">--defaults-group-suffix</code>]</td> <td>O prompt de comando a utilizar no<span><strong>MySQL</strong></span>Cliente de linha de comando.</td> </tr><tr> <td>[[<code>AUTHENTICATION_PAM_LOG</code><code>MYSQL_HISTFILE</code>]</td> <td>A senha padrão ao se conectar a<span><strong>- Não ,</strong></span>A utilização deste aparelho é insegura.</td> </tr><tr> <td>[[<code>CC</code><code>LD_RUN_PATH</code>]</td> <td>O número de porta TCP/IP por defeito.</td> </tr><tr> <td>[[<code>CC</code><code>LD_RUN_PATH</code>]</td> <td>O nome do arquivo de caminho de acesso do [[<code>CC</code><code>LIBMYSQL_ENABLE_CLEARTEXT_PLUGIN</code>].</td> </tr><tr> <td>[[<code>CC</code><code>mysql_clear_password</code>]</td> <td>Se o plug-in de rastreamento do protocolo de teste bloqueia os clientes.</td> </tr><tr> <td>[[<code>CC</code><code>LIBMYSQL_PLUGIN_DIR</code>]</td> <td>Se o plug-in de rastreamento do protocolo de ensaio produz resultados.</td> </tr><tr> <td>[[<code>CC</code><code>LIBMYSQL_PLUGINS</code>]</td> <td>Nome do arquivo de soquete padrão do Unix; usado para conexões com [[<code>CC</code><code>MYSQL_DEBUG</code>].</td> </tr><tr> <td>[[<code>CC</code><code>MYSQL_GROUP_SUFFIX</code>]</td> <td>O número de porta TCP/IP padrão do Plugin X.</td> </tr><tr> <td>[[<code>CC</code><code class="option">--defaults-group-suffix</code>]</td> <td>O nome padrão do arquivo de soquete do X Plugin; usado para conexões com o [[<code>CC</code><code>MYSQL_HISTFILE</code>].</td> </tr><tr> <td>[[<code>CXX</code><code>LD_RUN_PATH</code>]</td> <td>Soquete utilizado por<span><strong>- Não ,</strong></span>para comunicar com o Systemd.</td> </tr><tr> <td>[[<code>CXX</code><code>LD_RUN_PATH</code>]</td> <td>Usado pelo shell para encontrar programas MySQL.</td> </tr><tr> <td>[[<code>CXX</code><code>LIBMYSQL_ENABLE_CLEARTEXT_PLUGIN</code>]</td> <td>Localização de [[<code>CXX</code><code>mysql_clear_password</code>]<span><strong>pkg-config</strong></span>Ver nota no quadro seguinte.</td> </tr><tr> <td>[[<code>CXX</code><code>LIBMYSQL_PLUGIN_DIR</code>]</td> <td>O diretório no qual são criados ficheiros temporários.</td> </tr><tr> <td>[[<code>CXX</code><code>LIBMYSQL_PLUGINS</code>]</td> <td>Ver Secção B.3.3.7, "Problemas com os fusos horários".</td> </tr><tr> <td>[[<code>CXX</code><code>MYSQL_DEBUG</code>]</td> <td>O modo de criação de ficheiros do utilizador na criação de ficheiros.</td> </tr><tr> <td>[[<code>CXX</code><code>MYSQL_GROUP_SUFFIX</code>]</td> <td>O modo de criação de diretórios de utilizadores na criação de diretórios.</td> </tr><tr> <td>[[<code>CXX</code><code class="option">--defaults-group-suffix</code>]</td> <td>O nome de usuário padrão no Windows ao se conectar a<span><strong>- Não ,</strong></span></td> </tr></tbody></table>

Para obter informações sobre o arquivo de histórico do `mysql`, consulte a Seção 6.5.1.3, MySQL Client Logging.

O uso de `MYSQL_PWD` para especificar uma senha MySQL deve ser considerado *extremamente inseguro* e não deve ser usado. Algumas versões de `ps` incluem uma opção para exibir o ambiente de processos em execução. Em alguns sistemas, se você definir `MYSQL_PWD`, sua senha é exposta a qualquer outro usuário que execute `ps`. Mesmo em sistemas sem essa versão de `ps`, não é sábio supor que não existem outros métodos pelos quais os usuários possam examinar os ambientes de processo.

O `MYSQL_PWD` está desatualizado a partir do MySQL 8.4; espere que seja removido em uma versão futura do MySQL.

`MYSQL_TEST_LOGIN_FILE` é o nome de caminho do arquivo de caminho de login (o arquivo criado pelo **mysql\_config\_editor**). Se não definido, o valor padrão é `%APPDATA%\MySQL\.mylogin.cnf` diretório no Windows e `$HOME/.mylogin.cnf` em sistemas não Windows. Veja Seção 6.6.7, mysql\_config\_editor  MySQL Configuration Utility.

As variáveis `MYSQL_TEST_TRACE_DEBUG` e `MYSQL_TEST_TRACE_CRASH` controlam o plugin do cliente de rastreamento do protocolo de teste, se o MySQL for construído com esse plugin habilitado. Para mais informações, consulte Usando o Plugin de rastreamento do protocolo de teste.

Os valores padrão de `UMASK` e `UMASK_DIR` são `0640` e `0750`, respectivamente. O MySQL assume que o valor de `UMASK` ou `UMASK_DIR` está em octal se começa com um zero. Por exemplo, a configuração `UMASK=0600` é equivalente a `UMASK=384` porque 0600 octal é 384 decimal.

As variáveis `UMASK` e `UMASK_DIR`, apesar de seus nomes, são usadas como modos, não máscaras:

- Se `UMASK` for definido, `mysqld` usa `($UMASK | 0600)` como o modo para criação de arquivos, de modo que os arquivos recém-criados tenham um modo no intervalo de 0600 a 0666 (todos os valores são octal).
- Se `UMASK_DIR` for definido, `mysqld` usa `($UMASK_DIR | 0700)` como o modo base para criação de diretório, que é então AND-ed com `~(~$UMASK & 0666)`, de modo que diretórios recém-criados têm um modo na faixa de 0700 a 0777 (todos os valores octal). A operação AND pode remover permissões de leitura e gravação do modo de diretório, mas não executar permissões.

Ver também a secção B.3.3.1, "Problemas com permissões de ficheiro".

Pode ser necessário definir `PKG_CONFIG_PATH` se você usar `pkg-config` para criar programas MySQL. Veja Building C API Client Programs Using pkg-config.
