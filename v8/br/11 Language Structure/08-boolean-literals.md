### 11.1.6 Literais Booleanos

As constantes `TRUE` e `FALSE` valem `1` e `0`, respectivamente. Os nomes das constantes podem ser escritos em qualquer caso de letra.

```
mysql> SELECT TRUE, true, FALSE, false;
        -> 1, 1, 0, 0
```