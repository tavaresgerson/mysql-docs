#### 19.2.4.2 Repositórios de metadados de replicação

Um servidor de replicação cria dois repositórios de metadados de replicação: o repositório de metadados de conexão e o repositório de metadados do aplicável. Os repositórios de metadados de replicação sobrevivem ao desligamento de um servidor de replicação. Se a replicação baseada na posição do arquivo de log binário estiver em uso, quando a replicação for reiniciada, ela lê os dois repositórios para determinar até onde havia avançado anteriormente na leitura do log binário da fonte e no processamento do seu próprio log de retransmissão. Se a replicação baseada no GTID estiver em uso, a replicação não usa os repositórios de metadados de replicação para esse propósito, mas precisa deles para os outros metadados que contêm.

- O *repositório de metadados de conexão* da réplica contém informações que a thread de I/O de replicação (receptor) precisa para se conectar ao servidor de origem da replicação e recuperar transações do log binário da fonte. Os metadados neste repositório incluem a configuração de conexão, os detalhes da conta de usuário de replicação, as configurações SSL para a conexão e o nome do arquivo e a posição onde a thread do receptor de replicação está lendo atualmente do log binário da fonte.

- O repositório de metadados do *aplicador de réplicas* contém informações que o fio de SQL de replicação (aplicador) precisa ler e aplicar transações do log de retransmissão da réplica. Os metadados neste repositório incluem o nome do arquivo e a posição até a qual o fio de aplicador de replicação executou as transações no log de retransmissão, e a posição equivalente no log binário da fonte. Também inclui metadados para o processo de aplicação de transações, como o número de threads de trabalhador e a conta `PRIVILEGE_CHECKS_USER` para o canal.

O repositório de metadados de conexão é escrito na tabela `slave_master_info` no esquema do sistema `mysql` e o repositório de metadados do aplicativo é escrito na tabela `slave_relay_log_info` no esquema do sistema `mysql`. Uma mensagem de aviso é emitida se o **mysqld** não conseguir inicializar as tabelas dos repositórios de metadados de replicação, mas a replica é permitida para continuar iniciando. Esta situação ocorre com maior probabilidade ao atualizar de uma versão do MySQL que não suporta o uso de tabelas para os repositórios para uma versão em que elas são suportadas.

Importante

1. Não tente atualizar ou inserir linhas nas tabelas `mysql.slave_master_info` ou `mysql.slave_relay_log_info` manualmente. Isso pode causar comportamento indefinido e não é suportado. A execução de qualquer instrução que exija um bloqueio de escrita em uma ou ambas as tabelas `slave_master_info` e `slave_relay_log_info` é desaconselhada durante a replicação (embora instruções que realizam apenas leituras sejam permitidas a qualquer momento).

2. Os privilégios de acesso à tabela do repositório de metadados de conexão `mysql.slave_master_info` devem ser restritos ao administrador do banco de dados, pois ela contém o nome e a senha da conta de usuário de replicação para a conexão com a fonte. Use um modo de acesso restrito para proteger os backups do banco de dados que incluem essa tabela. A partir do MySQL 8.0.21, você pode limpar as credenciais da conta de usuário de replicação do repositório de metadados de conexão e, em vez disso, fornecê-las sempre usando a instrução `START REPLICA` ou a instrução `START GROUP_REPLICATION` que inicia o canal de replicação. Essa abordagem significa que o canal de replicação sempre precisa de intervenção do operador para ser reiniciado, mas o nome da conta e a senha não são registrados nos repositórios de metadados de replicação.

`RESET REPLICA` limpa os dados nos repositórios de metadados de replicação, com exceção dos parâmetros de conexão de replicação (dependendo da versão do MySQL Server). Para obter detalhes, consulte a descrição para `RESET REPLICA`.

A partir do MySQL 8.0.27, você pode definir a opção `GTID_ONLY` na instrução `CHANGE REPLICATION SOURCE TO` para impedir que um canal de replicação persista nomes de arquivos e posições de arquivos nos repositórios de metadados de replicação. Isso evita gravações e leituras nas tabelas em situações em que a replicação baseada em GTID não as exija realmente. Com a configuração `GTID_ONLY`, o repositório de metadados de conexão e o repositório de metadados do aplicável não são atualizados quando as filas de replicação e aplicam eventos em uma transação, ou quando os threads de replicação são parados e iniciados. As posições de arquivo são rastreadas na memória e podem ser visualizadas usando uma instrução `SHOW REPLICA STATUS` se forem necessárias. Os repositórios de metadados de replicação são sincronizados apenas nas seguintes situações:

- Quando uma declaração `CHANGE REPLICATION SOURCE TO` for emitida.

- Quando uma declaração `RESET REPLICA` é emitida. `RESET REPLICA ALL` exclui, em vez de atualizar, os repositórios, portanto, eles são sincronizados implicitamente.

- Quando um canal de replicação é inicializado.

- Se os repositórios de metadados de replicação forem movidos de arquivos para tabelas.

Antes do MySQL 8.0, para criar os repositórios de metadados de replicação como tabelas, era necessário especificar `master_info_repository=TABLE` e `relay_log_info_repository=TABLE` no início do servidor. Caso contrário, os repositórios eram criados como arquivos no diretório de dados com os nomes `master.info` e `relay-log.info`, ou com nomes e localizações alternativas especificados pela opção `--master-info-file` e pela variável de sistema `relay_log_info_file`. A partir do MySQL 8.0, a criação dos repositórios de metadados de replicação como tabelas é o padrão, e o uso de todas essas variáveis de sistema é desaconselhado.

As tabelas `mysql.slave_master_info` e `mysql.slave_relay_log_info` são criadas usando o mecanismo de armazenamento transacional `InnoDB`. As atualizações no repositório de metadados do aplicável são confirmadas junto com as transações, o que significa que as informações de progresso da replica registradas nesse repositório estão sempre consistentes com o que foi aplicado ao banco de dados, mesmo em caso de uma interrupção inesperada do servidor. Para obter informações sobre a combinação de configurações em uma replica que é mais resistente a interrupções inesperadas, consulte a Seção 19.4.2, “Tratamento de uma Interrupção Inesperada de uma Replica”.

Ao fazer o backup dos dados da replica ou transferir uma instantânea de seus dados para criar uma nova replica, certifique-se de incluir as tabelas `mysql.slave_master_info` e `mysql.slave_relay_log_info` que contêm os repositórios de metadados de replicação. Para operações de clonagem, observe que, quando os repositórios de metadados de replicação são criados como tabelas, eles são copiados para o destinatário durante uma operação de clonagem, mas quando são criados como arquivos, eles não são copiados. Quando a replicação baseada na posição do arquivo de log binário está em uso, os repositórios de metadados de replicação são necessários para retomar a replicação após o reinício da replica restaurada, copiada ou clonada. Se você não tiver os arquivos de log de relevo, mas ainda tiver o repositório de metadados do aplicável, você pode verificá-lo para determinar até que ponto o thread SQL de replicação foi executado no log binário da fonte. Em seguida, você pode usar uma declaração `CHANGE REPLICATION SOURCE TO` (a partir do MySQL 8.0.23) ou declaração `CHANGE MASTER TO` (antes do MySQL 8.0.23) com as opções `SOURCE_LOG_FILE` | `MASTER_LOG_FILE` e `SOURCE_LOG_POS` | `MASTER_LOG_POS` para dizer à replica para reler os logs binários da fonte a partir daquele ponto (desde que os logs binários necessários ainda existam na fonte).

Um repositório adicional, o repositório de metadados do trabalhador aplicador, é criado principalmente para uso interno e contém informações de status sobre os threads do trabalhador em uma replica multithread. O repositório de metadados do trabalhador aplicador inclui os nomes e posições do arquivo de log de retransmissão e do arquivo de log binário da fonte para cada thread do trabalhador. Se o repositório de metadados do aplicador for criado como uma tabela, o que é o padrão, o repositório de metadados do trabalhador aplicador é escrito na tabela `mysql.slave_worker_info`. Se o repositório de metadados do aplicador for escrito em um arquivo, o repositório de metadados do trabalhador aplicador é escrito no arquivo `worker-relay-log.info`. Para uso externo, as informações de status dos threads do trabalhador são apresentadas na tabela `replication_applier_status_by_worker` do Schema de Desempenho.

Os repositórios de metadados de replicação originalmente continham informações semelhantes às mostradas na saída da declaração `SHOW REPLICA STATUS`, que é discutida na Seção 15.4.2, “Declarações SQL para controle de servidores de replicação”. Informações adicionais foram posteriormente adicionadas aos repositórios de metadados de replicação, que não são exibidas pela declaração `SHOW REPLICA STATUS`.

Para o repositório de metadados de conexão, a tabela a seguir mostra a correspondência entre as colunas da tabela `mysql.slave_master_info`, as colunas exibidas pelo `SHOW REPLICA STATUS` e as linhas do arquivo desatualizado `master.info`.

<table summary="A correspondência entre as colunas da tabela mysql.slave_master_info, as colunas exibidas pelo comando SHOW REPLICA STATUS ou SHOW SLAVE STATUS e as linhas do arquivo master.info desatualizado."><thead><tr> <th scope="col">[[PH_HTML_CODE_<code>User_name</code>] Coluna da tabela</th> <th scope="col">[[PH_HTML_CODE_<code>User_name</code>] Coluna</th> <th scope="col">[[PH_HTML_CODE_<code>User_password</code>] Linha de arquivo</th> <th scope="col">Descrição</th> </tr></thead><tbody><tr> <th>[[PH_HTML_CODE_<code>SHOW REPLICA STATUS</code>]</th> <td>[Nenhu<code>User_name</code></td> <td>1</td> <td>Número de colunas na tabela (ou linhas no arquivo)</td> </tr><tr> <th>[[PH_HTML_CODE_<code>Port</code>]</th> <td>[[PH_HTML_CODE_<code>Source_Port</code>]</td> <td>2</td> <td>O nome do log binário atualmente sendo lido da fonte</td> </tr><tr> <th>[[PH_HTML_CODE_<code>Connect_retry</code>]</th> <td>[[PH_HTML_CODE_<code>Connect_Retry</code>]</td> <td>3</td> <td>A posição atual dentro do log binário que foi lido da fonte</td> </tr><tr> <th>[[PH_HTML_CODE_<code>Enabled_ssl</code>]</th> <td>[[PH_HTML_CODE_<code>Source_SSL_Allowed</code>]</td> <td>4</td> <td>O nome do host do servidor de origem da replicação</td> </tr><tr> <th>[[<code>User_name</code>]]</th> <td>[[<code>SHOW REPLICA STATUS</code><code>User_name</code>]</td> <td>5</td> <td>O nome da conta de usuário de replicação usado para se conectar à fonte</td> </tr><tr> <th>[[<code>User_password</code>]]</th> <td>Senha (não exibida por [[<code>SHOW REPLICA STATUS</code>]])</td> <td>6</td> <td>A senha da conta de usuário de replicação usada para se conectar à fonte</td> </tr><tr> <th>[[<code>Port</code>]]</th> <td>[[<code>Source_Port</code>]]</td> <td>7</td> <td>O porta de rede usado para se conectar ao servidor de origem de replicação</td> </tr><tr> <th>[[<code>Connect_retry</code>]]</th> <td>[[<code>Connect_Retry</code>]]</td> <td>8</td> <td>O período (em segundos) que a réplica espera antes de tentar se reconectar à fonte</td> </tr><tr> <th>[[<code>Enabled_ssl</code>]]</th> <td>[[<code>Source_SSL_Allowed</code>]]</td> <td>9</td> <td>Se a réplica suporta conexões SSL</td> </tr><tr> <th>[[<code>master.info</code><code>User_name</code>]</th> <td>[[<code>master.info</code><code>User_name</code>]</td> <td>10</td> <td>O arquivo usado para o certificado da Autoridade de Certificação (CA)</td> </tr><tr> <th>[[<code>master.info</code><code>User_password</code>]</th> <td>[[<code>master.info</code><code>SHOW REPLICA STATUS</code>]</td> <td>11</td> <td>O caminho para o certificado da Autoridade de Certificação (CA)</td> </tr><tr> <th>[[<code>master.info</code><code>Port</code>]</th> <td>[[<code>master.info</code><code>Source_Port</code>]</td> <td>12</td> <td>Nome do arquivo do certificado SSL</td> </tr><tr> <th>[[<code>master.info</code><code>Connect_retry</code>]</th> <td>[[<code>master.info</code><code>Connect_Retry</code>]</td> <td>13</td> <td>A lista de possíveis cifrados usados no handshake para a conexão SSL</td> </tr><tr> <th>[[<code>master.info</code><code>Enabled_ssl</code>]</th> <td>[[<code>master.info</code><code>Source_SSL_Allowed</code>]</td> <td>14</td> <td>Nome do arquivo de chave SSL</td> </tr><tr> <th>[[<code>Number_of_lines</code><code>User_name</code>]</th> <td>[[<code>Number_of_lines</code><code>User_name</code>]</td> <td>15</td> <td>Verificar o certificado do servidor</td> </tr><tr> <th>[[<code>Number_of_lines</code><code>User_password</code>]</th> <td>[Nenhu<code>User_name</code></td> <td>16</td> <td>Intervalo entre os batimentos cardíacos de replicação, em segundos</td> </tr><tr> <th>[[<code>Number_of_lines</code><code>SHOW REPLICA STATUS</code>]</th> <td>[[<code>Number_of_lines</code><code>Port</code>]</td> <td>17</td> <td>Qual das interfaces de rede da réplica deve ser usada para se conectar à fonte</td> </tr><tr> <th>[[<code>Number_of_lines</code><code>Source_Port</code>]</th> <td>[[<code>Number_of_lines</code><code>Connect_retry</code>]</td> <td>18</td> <td>A lista de IDs de servidor a serem ignorados. Observe que, para [[<code>Number_of_lines</code><code>Connect_Retry</code>], a lista de IDs de servidor é precedida pelo número total de IDs de servidor a serem ignorados.</td> </tr><tr> <th>[[<code>Number_of_lines</code><code>Enabled_ssl</code>]</th> <td>[[<code>Number_of_lines</code><code>Source_SSL_Allowed</code>]</td> <td>19</td> <td>O ID único da fonte</td> </tr><tr> <th>[[<code>Master_log_name</code><code>User_name</code>]</th> <td>[[<code>Master_log_name</code><code>User_name</code>]</td> <td>20</td> <td>Número máximo de tentativas de reconexão permitidas</td> </tr><tr> <th>[[<code>Master_log_name</code><code>User_password</code>]</th> <td>[Nenhu<code>User_name</code></td> <td>21</td> <td>Caminho para um arquivo de lista de revogação de certificado SSL</td> </tr><tr> <th>[[<code>Master_log_name</code><code>SHOW REPLICA STATUS</code>]</th> <td>[Nenhu<code>User_name</code></td> <td>22</td> <td>Caminho para um diretório contendo arquivos da lista de revogação de certificados SSL</td> </tr><tr> <th>[[<code>Master_log_name</code><code>Port</code>]</th> <td>[[<code>Master_log_name</code><code>Source_Port</code>]</td> <td>23</td> <td>Se o autoposicionamento do GTID está em uso ou não</td> </tr><tr> <th>[[<code>Master_log_name</code><code>Connect_retry</code>]</th> <td>[[<code>Master_log_name</code><code>Connect_Retry</code>]</td> <td>24</td> <td>O nome do canal de replicação</td> </tr><tr> <th>[[<code>Master_log_name</code><code>Enabled_ssl</code>]</th> <td>[[<code>Master_log_name</code><code>Source_SSL_Allowed</code>]</td> <td>25</td> <td>Versão TLS na fonte</td> </tr><tr> <th>[[<code>Source_Log_File</code><code>User_name</code>]</th> <td>[[<code>Source_Log_File</code><code>User_name</code>]</td> <td>26</td> <td>Nome do arquivo de chave pública RSA</td> </tr><tr> <th>[[<code>Source_Log_File</code><code>User_password</code>]</th> <td>[[<code>Source_Log_File</code><code>SHOW REPLICA STATUS</code>]</td> <td>27</td> <td>Se solicitar a chave pública RSA da fonte</td> </tr><tr> <th>[[<code>Source_Log_File</code><code>Port</code>]</th> <td>[[<code>Source_Log_File</code><code>Source_Port</code>]</td> <td>28</td> <td>Espaço de rede</td> </tr><tr> <th>[[<code>Source_Log_File</code><code>Connect_retry</code>]</th> <td>[Nenhu<code>User_name</code></td> <td>29</td> <td>Algoritmos de compressão permitidos para a conexão com a fonte</td> </tr><tr> <th>[[<code>Source_Log_File</code><code>Connect_Retry</code>]</th> <td>[Nenhu<code>User_name</code></td> <td>30</td> <td>Nível de compressão [[<code>Source_Log_File</code><code>Enabled_ssl</code>]</td> </tr><tr> <th>[[<code>Source_Log_File</code><code>Source_SSL_Allowed</code>]</th> <td>[Nenhu<code>User_name</code></td> <td>31</td> <td>Suítes de cifra permitidas para TLSv1.3</td> </tr><tr> <th>[[<code>Master_log_pos</code><code>User_name</code>]</th> <td>[Nenhu<code>User_name</code></td> <td>32</td> <td>Se o mecanismo de failover de conexão assíncrona estiver ativado</td> </tr><tr> <th>[[<code>Master_log_pos</code><code>User_name</code>]</th> <td>[Nenhu<code>User_name</code></td> <td>33</td> <td>Se o canal usa apenas GTIDs e não persistir posições</td> </tr></tbody></table>

Para o repositório de metadados do aplicativo, a tabela a seguir mostra a correspondência entre as colunas da tabela `mysql.slave_relay_log_info`, as colunas exibidas pelo `SHOW REPLICA STATUS` e as linhas do arquivo desatualizado `relay-log.info`.

<table summary="A correspondência entre as colunas da tabela mysql.slave_relay_log_info, as colunas exibidas pelo comando SHOW REPLICA STATUS ou SHOW SLAVE STATUS e as linhas do arquivo relay-log.info desatualizado."><thead><tr> <th scope="col">[[PH_HTML_CODE_<code>Master_log_pos</code>] Coluna da tabela</th> <th scope="col">[[PH_HTML_CODE_<code>Master_log_pos</code>] Coluna</th> <th scope="col">Linha no arquivo [[PH_HTML_CODE_<code>Sql_delay</code>]</th> <th scope="col">Descrição</th> </tr></thead><tbody><tr> <th>[[PH_HTML_CODE_<code>SQL_Delay</code>]</th> <td>[Nenhu<code>Master_log_pos</code></td> <td>1</td> <td>Número de colunas na tabela ou linhas no arquivo</td> </tr><tr> <th>[[PH_HTML_CODE_<code>Number_of_workers</code>]</th> <td>[[PH_HTML_CODE_<code>Id</code>]</td> <td>2</td> <td>O nome do arquivo de registro atual do relé</td> </tr><tr> <th>[[PH_HTML_CODE_<code>Channel_name</code>]</th> <td>[[PH_HTML_CODE_<code>Channel_name</code>]</td> <td>3</td> <td>A posição atual dentro do arquivo de registro do relé; os eventos até essa posição foram executados no banco de dados da replica</td> </tr><tr> <th>[[PH_HTML_CODE_<code>Privilege_checks_username</code>]</th> <td>[[PH_HTML_CODE_<code>PRIVILEGE_CHECKS_USER</code>]</td> <td>4</td> <td>O nome do arquivo de log binário da fonte a partir do qual os eventos no arquivo de log de retransmissão foram lidos</td> </tr><tr> <th>[[<code>Master_log_pos</code>]]</th> <td>[[<code>SHOW REPLICA STATUS</code><code>Master_log_pos</code>]</td> <td>5</td> <td>A posição equivalente dentro do arquivo de log binário da fonte dos eventos que foram executados na replica</td> </tr><tr> <th>[[<code>Sql_delay</code>]]</th> <td>[[<code>SQL_Delay</code>]]</td> <td>6</td> <td>O número de segundos que a réplica deve ficar atrasada em relação à fonte</td> </tr><tr> <th>[[<code>Number_of_workers</code>]]</th> <td>[Nenhu<code>Master_log_pos</code></td> <td>7</td> <td>Número de threads de trabalhador para aplicar transações de replicação em paralelo</td> </tr><tr> <th>[[<code>Id</code>]]</th> <td>[Nenhu<code>Master_log_pos</code></td> <td>8</td> <td>ID usado para fins internos; atualmente, este é sempre 1</td> </tr><tr> <th>[[<code>Channel_name</code>]]</th> <td>[[<code>Channel_name</code>]]</td> <td>9</td> <td>O nome do canal de replicação</td> </tr><tr> <th>[[<code>Privilege_checks_username</code>]]</th> <td>[Nenhu<code>Master_log_pos</code></td> <td>10</td> <td>O nome de usuário da conta [[<code>PRIVILEGE_CHECKS_USER</code>]] para o canal</td> </tr><tr> <th>[[<code>relay-log.info</code><code>Master_log_pos</code>]</th> <td>[Nenhu<code>Master_log_pos</code></td> <td>11</td> <td>O nome do host da conta [[<code>relay-log.info</code><code>Master_log_pos</code>] para o canal</td> </tr><tr> <th>[[<code>relay-log.info</code><code>Sql_delay</code>]</th> <td>[Nenhu<code>Master_log_pos</code></td> <td>12</td> <td>Se o canal aceita apenas eventos baseados em linhas</td> </tr><tr> <th>[[<code>relay-log.info</code><code>SQL_Delay</code>]</th> <td>[Nenhu<code>Master_log_pos</code></td> <td>13</td> <td>A política do canal sobre se as tabelas devem ter chaves primárias para as operações [[<code>relay-log.info</code><code>Number_of_workers</code>] e [[<code>relay-log.info</code><code>Id</code>]</td> </tr><tr> <th>[[<code>relay-log.info</code><code>Channel_name</code>]</th> <td>[Nenhu<code>Master_log_pos</code></td> <td>14</td> <td>Se o canal atribuir um GTID a transações replicadas que ainda não possuem um, usando o UUID local da replica, esse valor é [[<code>relay-log.info</code><code>Channel_name</code>]; se o canal fizer isso usando, em vez disso, um UUID que foi definido manualmente, o valor é [[<code>relay-log.info</code><code>Privilege_checks_username</code>]. Se o canal não atribuir um GTID nesses casos, o valor é [[<code>relay-log.info</code><code>PRIVILEGE_CHECKS_USER</code>].</td> </tr><tr> <th>[[<code>Number_of_lines</code><code>Master_log_pos</code>]</th> <td>[Nenhu<code>Master_log_pos</code></td> <td>15</td> <td>O UUID usado nos GTIDs atribuídos a transações anônimas</td> </tr></tbody></table>
