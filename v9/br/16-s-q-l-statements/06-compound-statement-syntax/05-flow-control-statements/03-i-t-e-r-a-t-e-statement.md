#### 15.6.5.3 Declaração `ITERATE`

```
ITERATE label
```

A palavra `ITERATE` pode aparecer apenas dentro das declarações `LOOP`, `REPEAT` e `WHILE`. `ITERATE` significa “começar o loop novamente”.

Para um exemplo, veja a Seção 15.6.5.5, “Declaração `LOOP`”.