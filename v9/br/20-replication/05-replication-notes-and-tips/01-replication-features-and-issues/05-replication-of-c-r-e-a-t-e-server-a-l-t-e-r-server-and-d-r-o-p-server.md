#### 19.5.1.5 Replicação de CREATE SERVER, ALTER SERVER e DROP SERVER

As instruções `CREATE SERVER`, `ALTER SERVER` e `DROP SERVER` não são escritas no log binário, independentemente do formato de registro binário que estiver em uso.