### 25.7.1 Replicação de Clusters do NDB: Abreviações e Símbolos

Ao longo desta seção, usamos as seguintes abreviações ou símbolos para referir-se aos clusters fonte e replica, e aos processos e comandos executados nos nós do cluster:

**Tabela 25.41 Abreviações usadas ao longo desta seção para referir-se aos clusters fonte e replica, e aos processos e comandos executados nos nós do cluster**

<table><thead><tr> <th>Símbolo ou Abreviação</th> <th>Descrição (Refere-se a...)</th> </tr></thead><tbody><tr> <td><em class="replaceable"><code>S</code></em></td> <td>O clúster que atua como a (fonte) de replicação primária</td> </tr><tr> <td><em class="replaceable"><code>R</code></em></td> <td>O clúster que atua como a (replica) primária</td> </tr><tr> <td><code>shell<em class="replaceable"><code>S</code></em>&gt;</code></td> <td>Comando de shell a ser executado no clúster de origem</td> </tr><tr> <td><code>mysql<em class="replaceable"><code>S</code></em>&gt;</code></td> <td>Comando do cliente MySQL executado em um único servidor MySQL que funciona como um nó SQL no clúster de origem</td> </tr><tr> <td><code>mysql<em class="replaceable"><code>S*</code></em>&gt;</code></td> <td>Comando do cliente MySQL a ser executado em todos os nós SQL que participam do clúster de origem de replicação</td> </tr><tr> <td><code>shell<em class="replaceable"><code>R</code></em>&gt;</code></td> <td>Comando de shell a ser executado no clúster de replicação</td> </tr><tr> <td><code>mysql<em class="replaceable"><code>R</code></em>&gt;</code></td> <td>Comando do cliente MySQL executado em um único servidor MySQL que funciona como um nó SQL no clúster de replicação</td> </tr><tr> <td><code>mysql<em class="replaceable"><code>R*</code></em>&gt;</code></td> <td>Comando do cliente MySQL a ser executado em todos os nós SQL que participam do clúster de replicação</td> </tr><tr> <td><em class="replaceable"><code>C</code></em></td> <td>Canal de replicação primário</td> </tr><tr> <td><em class="replaceable"><code>C'</code></em></td> <td>Canal de replicação secundário</td> </tr><tr> <td><em class="replaceable"><code>S'</code></em></td> <td>Fonte de replicação secundária</td> </tr><tr> <td><em class="replaceable"><code>R'</code></em></td> <td>Replica secundária</td> </tr></tbody></table>