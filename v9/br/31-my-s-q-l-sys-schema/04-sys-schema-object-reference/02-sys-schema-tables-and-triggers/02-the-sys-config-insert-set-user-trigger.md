#### 30.4.2.2 O gatilho sys_config_insert_set_user

Para as linhas adicionadas à tabela `sys_config` por instruções `INSERT`, o gatilho `sys_config_insert_set_user` define a coluna `set_by` para o usuário atual.